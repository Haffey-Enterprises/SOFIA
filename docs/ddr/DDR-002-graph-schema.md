# File: docs/ddr/DDR-002-graph-schema.md
# Author: Thaddeus Haffey — Executive Architect, Haffey Enterprises LLC
# Created: 2026-06-21
# Description: DDR-002 — Graph Schema. Fixes the node / relationship / constraint / index contract for the SOFIA graph system-of-record — the five-core-plane-plus-Extension Knowledge Graph, the Reasoning Graph, and the Artifact node-family — implementing DDR-001's data architecture at the schema layer (the schema half of the architecture/schema split).

# DDR-002 — Graph Schema: Node, Relationship, Constraint, and Index Contract

| Field | Value |
|---|---|
| **Document ID** | DDR-002 |
| **Version** | 1.6.0 |
| **Status** | ACCEPTED |
| **Date** | 2026-07-18 |
| **Authors** | Thaddeus Haffey (Executive Architect) |
| **Supersedes** | None — new design ruling. |
| **References** | ADR-001 v1.2.0; ADR-002 v1.2.0; ADR-008 v1.0.0; DDR-001 v1.4.0 |

---

## Decision

DDR-002 fixes the **node / relationship / constraint / index contract** for the SOFIA graph system-of-record. It owns the schema; it does **not** own architecture (DDR-001), feedback-loop governance (DDR-003), or service realization (the SDDs). Decomposed into testable ruling components:

- **Decision.1** — The schema defines the node, relationship, constraint, and index contract for the **Knowledge Graph (KG)**, the **Reasoning Graph (RG)**, and the **Artifact node-family**, co-resident in one Neo4j Enterprise instance (DDR-001 Decision.1).
- **Decision.2** — The KG is the **five core planes** fixed by DDR-001 (Catalog, Environment, Operational, Governance, Standards) **plus the Extension plane**, where cost registers as the first Extension registration — cost is **not** a sixth core plane.
- **Decision.3** — The RG is **captured reasoning**, cross-graph-linked to the KG, and is **not** a KG plane (carries no plane label).
- **Decision.4** — The **Artifact family** (produced deliverables — solutions, BCDR (business-continuity / disaster-recovery) plans, build sheets) is a node-family *within* the two-graph store — **neither a KG plane nor a third graph** — joined to the KG and RG only by edges.
- **Decision.5** — The schema is bound by a **two-axis provenance model** (`origin_mechanism` × `derivation_class`), a **reversibility-tiered completeness posture**, DB-enforced uniqueness/existence constraints, and a **CI (continuous-integration)-only conformance-invariant set** with a bound, named exposure window.
- **Decision.6** — This DDR owns the schema contract only; architecture (DDR-001), feedback-loop governance (DDR-003), and service realization (the SDDs) are routed, not authored, here.
- **Decision.7** — Un-promotion of a promoted KG fact is the **retraction shape**: an Enterprise Architect (EA)-gated reversing `CandidatePromotion` (`proposal_kind: retraction`) + `RETRACTS` edge, through the same EA gate as a forward promotion (§5; §7 #21, #25).
- **Decision.8** — A promoted node's provenance survives `Evidence` retention via the **provenance-survival guarantee**: a `(:Reasoning:ProvenanceSummary)` materialized **at promotion**, complete against the §5-defined originating-`Evidence` span (§4/§5; §7 #20).
- **Decision.9** — Promoted knowledge carries the **`applicability_state` conditional-consumption model**: a `conditional` node is admitted only where the consuming context satisfies its `Condition`, with per-version supersession carry-forward (§5; §7 #19, #22).
- **Decision.10** — Versioned ground truth follows **Option A supersession** — retained version nodes + effective-dating + `superseded_by`, atomic re-bind (§6).
- **Decision.11** — Confidence-derivation totality is a **per-class property established at the definition boundary**: every node-class declares exactly one confidence-derivation basis — in §2.1–2.5 for core planes, in the `PlaneDefinition` registration contract for Extension planes (§2.6) — so a well-formed citation either derives or rejects typed, and `CONFIDENCE_UNDERIVABLE` (the typed fail-closed rejection, §2) is unreachable for any well-formed class (§2; §7 #26, #27). Derivation semantics and values are DDR-004's.

---

## Rationale

This ruling is **deliberation-grounded**: it is the output of the pre-authoring design session, which deliberated the node/edge/constraint shape plane by plane against DDR-001's ratified data architecture and ADR-001/ADR-002's reasoning-and-system-of-record commitments. The design space, the alternatives weighed at each plane, and the disposition of every review finding were worked through in that session; this DDR carries the settled contract.

The completeness posture is deliberately **reversibility-scoped, not a uniform dial**: identity keys, constraints, edge-binding properties, and invariant/traversal-bearing properties are locked now because they are expensive to reverse downstream (DDR-002 gates the downstream service SDDs and is cited by DDR-003); descriptive and rich application payload stay schema-on-write and SDD-level. Where the design cycle surfaced a mechanism with **no empirical instance yet** — and locking it would over-specify — the capability is named as a gap (see *Named Gaps*) and re-instated by additive amendment when a real instance arises, rather than carried speculatively. This is the empirical-warrant discipline that governs what is locked versus named.

---

## 1. Schema grammar & global conventions

**Naming.** Node labels `PascalCase`; relationship types `SCREAMING_SNAKE`; properties `snake_case`; primary keys `<entity>_id`.

**Provenance (origin-neutral group; two-axis).** Every **KG** node carries a provenance group — minimally `origin_mechanism` + `recorded_at` (and `source_record_ref` where applicable); DB-existence-constrained (§7). Provenance is split into **two orthogonal axes**:

- **`origin_mechanism`** (how the node entered — `ingested` external ground truth · `authored` human/SOFIA-authored · `promoted` materialized through the feedback-loop EA gate · `derived` produced by an internal process), always present and invariant-bearing;
- **`derivation_class`** (the nature of the content — `primary` · `distilled` · `aggregated`), present where it carries information.

The two axes form the **provenance scoping matrix**: `ingested → primary` · `authored → primary` · `derived → distilled | aggregated` · **`promoted → N/A`**. `derivation_class` is **N/A for `promoted`** by design: a promoted node carries `origin_mechanism: promoted` as its provenance-axis fact, and its pre-promotion *content nature* (was the source distilled? aggregated? authored reasoning?) lives **at the source node**, recovered by traversing the provenance chain (§5) — never denormalized onto the promoted node. This de-tangles a process-vs-nature conflation: a distilled `ObservedPattern` and a computed `CapabilityCostEstimate` each become independently queryable on both axes — (`origin_mechanism: derived`, `derivation_class: distilled`) and (`origin_mechanism: derived`, `derivation_class: aggregated`) respectively. **Matrix note:** a **SOFIA-recorded human decision** — an EA's approval act made in SOFIA, e.g. a `PromotionDecision` (§2.4) — is the **`authored → primary`** case: `authored` because SOFIA is the issuing authority (no external record to capture), `primary` because the decision content is original, neither distilled nor aggregated.

**`source_record_ref`** is the structured, durable reference to the originating external system of record — `{source_system_class, record_id}` (system named by *class*, not a vendor product, per DDR-001's source-named-by-class convention). The graph holds the reference, not the record: **required on `ingested`** and on **`derived` with `derivation_class: distilled`** (a distilled `ObservedPattern` references its telemetry source — aligns §7 check #17); **permitted but not required on `aggregated`** `derived` nodes (which derive from internal version-pins, not an external record); **N/A on `authored`/`promoted`** (provenance is internal lineage).

**`recorded_at`** is the uniform system/record time (when the graph captured the node); domain timestamps (`decided_at`, `first_observed_at`/`last_observed_at`, `computed_at`, `generated_at`) are node-type-specific and never conflated with it.

The `promoted` vs. `ingested` distinction — a **DDR-001 conformance-check #6 obligation** (promoted knowledge must be distinguishable from ingested) — is carried by `origin_mechanism`. RG and Artifact provenance differ (§4, §5).

**Provenance scope.** The provenance *existence constraint* (§7) scopes to: **KG nodes**, the **three authored RG types** (`ReasoningSession`, `ReasoningProgress`, `RejectedAlternative`, all `origin_mechanism: authored`), the **derived, group-carrying `ProvenanceSummary`** (`origin_mechanism: derived`; §4), and the **Artifact family** (§5). `RejectedAlternative` carries the group as authored content because DDR-001 frames it as ASA (Architecture Solutioning Agent)-authored and the *primary feedback-loop input* (§Reasoning Graph) — the highest-leverage promotion signal must not carry the weakest provenance. **`Evidence` is the sole surrogate-only RG type:** its provenance is recovered structurally via `SOURCED_FROM` (the atomic capture-unit, §7), not a carried group (§4).

**No vectors — both halves.** *Negative half:* no node or edge carries vector/embedding properties (documented invariant + conformance check). *Positive half:* the graph-native retrieval substrate that *replaces* vector similarity is the existing structure — the `l1/l2/l3` capability/pattern taxonomy (§2.1), the selection-edge web (§3), and the track-record subgraph (§2.3). DDR-002 **names** these as the no-vector substitute and restates the no-vector prohibition as binding; the concrete **retrieval affordance** (graph-traversal query patterns; whether a `SIMILAR_TO` edge is warranted and how it would be populated without embeddings) is **routed to the knowledge-service / solutioning SDDs**, not designed here.

**Classification / no-PHI.** Data classification is gateway-enforced at ingestion; SOFIA stores no PHI (protected-health-information) *data*. Note the distinction: PHI/PCI *policies* are legitimate Standards knowledge (rules about architectures that handle such data); classification strips the data, not the rules.

**Plane membership — hybrid (KG-scoped).** Every **KG** node carries a **secondary plane label** (enables cheap label-scoped traversal) **and** the plane is declared in a `PlaneDefinition` registry node (§2.6). Core planes are fixed by DDR-001; the registry is the Extension surface. **RG nodes and Artifact-family nodes carry no KG plane label** — they are not plane citizens; they are reached by cross-graph traversal (§5). This exemption is what lets plane-scoped ground-truth traversals skip reasoning state and produced artifacts structurally.

**Completeness posture — the tiering that governs every node/edge.** Scope by *reversibility*, not a uniform dial:

- **T1** — identity / structural keys, constraints, edge-binding properties. *Expensive to reverse → complete now.*
- **T2** — invariant- or traversal-bearing properties (filters, discriminators, version pins, status). *Drives behavior → complete now, indexed where it drives traversal.*
- **T3** — descriptive payload. *Cheap to add (schema-on-write) → minimal now, gaps named not padded.*
- **T4** — rich application payload. *SDD-level, not schema.*

**Contested-T2 annotation.** Where a T2 value-set is **POC (proof-of-concept)-asserted** rather than invariant-grounded, it is flagged *(contested)* so a spike-generalization review pass can revisit it. Current contested T2s: `conclusion_type` enum (§4), `GateDecision.gate` enum (§2.4), `polarity` neutral option (§2.3), `basis_strength` (§5). Invariant-grounded T2s (plane labels, version pins, status discriminators) are not flagged.

**Denormalization boundary (consolidated).** Cross-plane concerns stay in their owning plane, reached by traversal — never duplicated onto a node. The "never duplicated" rule governs **hot-path live-fact duplication**; it is **not** breached by two bounded, justified kinds, named here once so neither reads as a local exception:

- **Point-in-time / retention-boundary snapshots** — `Evidence.fact_summary` + `source_node_version` pins (§4), and the frozen provenance snapshot that preserves a promoted node's lineage past `Evidence` expiry (the `(:Reasoning:ProvenanceSummary)` node, §4/§5). These capture *what a fact said at a point in time*, precisely because the live source can drift or be deliberately retained-out — a **temporal-fidelity** concern the never-duplicated rule does not govern (its "traverse to the live home" presupposes a live home).
- **Sync-checked edge-mirrors** — `dependency_manifest` (`PolicyRule`, §2.5) mirrors `GOVERNED_BY`/`MANDATES` structure to make opaque-logic reads introspectable, kept honest by §7 conformance check #10 (the mirror must match the edges) and its declared-not-undeclared caveat (§7). A `Condition`'s manifest (§2.4) is declared-introspectable without an edge mirror — consumed at retrieval-time evaluation (§7 #19, #10).

---

## 2. The planes — node schemas

Property tier in parentheses. `provenance (T1)` denotes the provenance group. §2.1–§2.5 are the five core planes fixed by DDR-001; §2.6 is the Extension surface, where cost registers first.

**Confidence-derivation basis (per-class declaration).** Every node-class in §2.1–§2.6 declares exactly one confidence-derivation basis at its definition boundary — for core planes here in §2.1–§2.5, for Extension planes in the `PlaneDefinition` registration contract (§2.6). The four bases: **`native_confidence`** (basis 1 — the node carries its own `confidence`; the citation inherits it) · **`flat_base`** (basis 2 — non-aging; staleness governed by supersession/lifecycle, no decay) · **`aging`** (basis 3 — class base × freshness decay over a declared domain freshness operand) · **`non_citable`** (basis 4 — not a valid `Evidence` source; a citation rejects typed at capture, §2.6). **Resolution rule:** a plane-level declaration governs every node-label in its section absent a per-label override; every node-label resolves exactly one basis. **The citable universe is exact:** `SOURCED_FROM` targets KG nodes only (§5), and Reasoning and Artifact nodes carry no KG plane label — so the declarations in §2.1–§2.6 cover the citable universe completely, and `CONFIDENCE_UNDERIVABLE` is unreachable for any well-formed class (defense-in-depth only; DDR-004). **A declared basis governs the derivation when a node of that class is cited; it imposes no requirement that such citations exist — evidence completeness is never a derivation concern.** Declarations carry basis kind (and, for `aging`, the freshness operand) only — no values: derivation semantics, base values, and their invariant-vs-contested treatment are DDR-004's.

### 2.1 Catalog — the selection graph (versioned ground truth)

- **`(:Catalog:Pattern)`** — `pattern_id` (T1 PK) · `version` (T1) · `pattern_type` (T2, indexed) · `status` (T2, indexed, versioned) · `effective_from`/`superseded_by` (T2) · `name` (T3) · provenance (T1)
- **`(:Catalog:Capability)`** — `capability_id` (T1 PK) · `version` (T1) · `name` (T3) · `l1_taxonomy`/`l2_taxonomy`/`l3_taxonomy` (T2) · `status` (T2) · `description` (T3) · provenance (T1)
- **`(:Catalog:Technology)`** — `technology_id` (T1 PK) · `version` (T1) · `name` (T3) · `vendor` (T3) · `platform` (T3) · `approval_status` (T2, indexed) · `version_constraint`/`min`/`max` (T2) · `deprecation_date` (T2) · `tier_applicability[]` (T2) · `approved_data_classifications[]` (T2) · provenance (T1)
- **`(:Catalog:IacTemplate)`** — `iac_template_id` (T1 PK) · `template_type` (T2) · `name` (T3) · `version` (T1) · `status` (T2) · provenance (T1)

*Confidence-derivation basis:* **`flat_base`** (basis 2), plane-level — versioned authoritative ground truth; staleness is supersession, not decay (§2 preamble; DDR-004).

The `l1/l2/l3` taxonomy + the selection edges are the **graph-native retrieval substrate** (no-vector positive half, §1) — affordance design → knowledge-service / solutioning SDDs.

*Reference vs. produced.* "Solution documents" as *reference* Catalog content (template/exemplar solutions, library knowledge) are distinct from the **produced** `(:Artifact:Solution)` keystone (§5). A reference solution is Catalog ground truth; a produced solution is an Artifact. They are not the same node, and Catalog never holds the produced ASD (Architecture Solution Document, §5/DDR-001).

### 2.2 Environment — the as-running graph (ages via freshness)

- **`(:Environment:DeployedService)`** — `deployed_service_id` (T1 PK) · `service_type` (T3) · `lifecycle_state` (T2, indexed) · `observed_at` (T2) · provenance (T1, `origin_mechanism: ingested` · `derivation_class: primary`, runtime/CMDB (configuration-management-database) source class)
- **`(:Environment:DeploymentEnvironment)`** — `environment_id` (T1 PK) · `name` (T3) · `environment_class` (T2, indexed — production/staging/dev) · provenance (T1). ***Per-label basis override:* `flat_base` (basis 2)** — the plane's structurally-stable member: a realized environment *identity*, which does not age. Its absence of `observed_at` is **correct by design, not a schema gap** — a freshness field would model non-existent staleness, and the option "add `observed_at`" stands rejected (DDR-004).
- **`(:Environment:ConfigurationItem)`** — `ci_id` (T1 PK) · `ci_type` (T2, indexed) · `name` (T3) · `observed_at` (T2) · provenance (T1)

*Plane signature:* freshness (`observed_at`) — runtime facts are less authoritative than catalog ground truth; this lets the RG weight environment evidence lower. *Confidence-derivation basis:* **`aging`** (basis 3), plane-level, freshness operand `observed_at` — with the `DeploymentEnvironment` per-label override above (structural identity is the signature's one exception: it does not age). *Lifecycle distinction:* `DeploymentEnvironment` is the **realized** runtime environment, distinct from a solution's design-time `target_environment` (§5). Environment is DDR-001-sanctioned CMDB-sourced ground truth.

### 2.3 Operational — the track-record graph (durable distillation, not telemetry)

- **`(:Operational:ObservedPattern)`** — `observed_pattern_id` (T1 PK) · `polarity` (T2, indexed — `strength` / `weakness` / optional `neutral` *(contested)*) · `pattern_type` (T2, indexed) · `description` (T3) · `observation_window` (T3) · `confidence` (T2 — **lesson reliability**; see §4 canonical def; per-target observation certainty rides the `OBSERVED_IN` edge, §3) · `first_observed_at`/`last_observed_at` (T2) · `status` (T2, indexed — active/superseded/resolved/**archived**) · provenance (T1, `origin_mechanism: derived` · `derivation_class: distilled`)

*Confidence-derivation basis:* **`native_confidence`** (basis 1), plane-level — the plane's authority measure is the node's own `confidence` (lesson reliability); a citation inherits it.

The KG does **not** mirror transient SoR (system-of-record) telemetry. It holds the durable distilled lesson — a recurring operational pattern, of **dual polarity** (a clean track record is as informative as a weakness). **No in-graph TTL.** Raw telemetry stays TTL-bounded in the **external** observability/AIOps SoR; it is not a KG plane and is not mirrored. **Bounded by distinctness.** Distillation is **update-in-place** — the *active* `ObservedPattern` set is bounded by the number of distinct durable lessons (each updated as evidence accrues via `first_observed_at`/`last_observed_at`/`confidence`), **not** a node-per-observation; the never-delete posture is qualified to *never silently delete*, with terminal-state (`resolved`/`superseded`) patterns transitioning to **`archived`** per **DDR-003** policy. `archived` is **retention-tier demotion, never hard-deletion** — an archived pattern stays traceable and is reversible if a dormant pattern recurs. DDR-002 commits the `archived` `status` value; the archival **policy** (the terminal-state-plus-dwell trigger, cadence) remains DDR-003's. The distillation write's **checkpoint posture is explicitly un-decided**: whether writing a `derived` `ObservedPattern` into the Operational plane warrants a human-accountable checkpoint before entry is routed to **ADR-008 (Ground-Truth Mutation Governance)** (see *Cross-References*, the boundary routing map), not asserted here. The distillation step (reading the SoR, generalizing patterns) writes `ObservedPattern` with `origin_mechanism: derived` / `derivation_class: distilled` and a `source_record_ref` to the external telemetry source; the process itself (cadence, generalization criteria, ownership) → **DDR-003 / detection-promotion SDD**. Weakness/strength entry into the KG is **EA-gated** via the feedback loop (§5), not auto-ingested. *This plane aligns to DDR-001's Operational-plane definition (durable distilled `ObservedPattern`s).*

### 2.4 Governance — the decision/audit graph (immutable append-only; references, not records)

**Decision supertype + subtypes.** A single shared decision *shape* with two subtype labels, so SDLC (software-development-lifecycle) gates and the feedback-loop promotion gate share structure without being conflated. **Subtype integrity:** every `Decision` carries **exactly one** subtype label — never bare, never both — enforced as a CI-only invariant (§7; Neo4j cannot enforce label cardinality natively). The **approving-state set** (referenced by the promoted-origin invariant, §7) is `{approved, approved_conditional}`.

- **`(:Governance:Decision)`** *(supertype shape — never instantiated bare; every decision node also carries a subtype label)* — `decision_id` (T1 PK) · `decided_at` (T2) · `approval_token_id` (T3) · provenance (T1). Decided-by via `(Actor)-[:APPROVED]->(Decision)`. **The verdict is not a supertype property:** it rides the `DECIDED_ON {outcome ∈ approved | approved_conditional | rejected}` edge (§3, §5) — a property of *the decision's act on each target* — so a `GateDecision` carries one outcome and a batch `PromotionDecision` carries a per-candidate outcome on each edge, with no single-value supertype field to break on batches and no double-record.
- **`(:Governance:Decision:GateDecision)`** *(SDLC gates)* — the **enterprise SDLC gate on produced Solutions**: the external authority decides, and SOFIA mirrors the outcome as a captured reference. Adds `gate` (T2, indexed — SDLC gate discriminator; **placeholder `gate_0`/`gate_1`/`gate_2` (contested) → reconcile to enterprise gate taxonomy**) · the standard provenance group with **`origin_mechanism: ingested`** (the external-captured gate — the only grounded case; §7 check #17 governs its `source_record_ref` obligation) · **`source_record_ref`** `{source_system_class, record_id}` (the standard structured external reference, per §1) · `all_hard_constraints_passed` (T2) · `rejection_reason` (T3, conditional). `DECIDED_ON {outcome}` → **Solution** (§5). The decided-in-SOFIA mechanics — a future human approval *issued* in SOFIA, or a SOFIA-issued approval — are routed by name to the **approval + governance-state-manager SDDs**; the origin-evolution path is the consolidated *gate-decision origin evolution* Named Gap (below).
- **`(:Governance:Decision:PromotionDecision)`** *(feedback-loop EA approval — SOFIA-issued)* — **`origin_mechanism: authored`** (the approving `Actor` is a human EA; SOFIA is the issuing authority) and therefore **no `source_record_ref`** — re-grounded on the §1 provenance matrix's `authored → primary` case (a SOFIA-recorded human decision), not an ad-hoc exception; §7 check #17 exempts `authored`. `DECIDED_ON {outcome}` → **CandidatePromotion** (§5), **1:many** (a single batch decision may decide multiple candidates — the **per-candidate verdict lives on each `DECIDED_ON {outcome}` edge** — so one decision can approve some, reject others, and conditionally-approve others; `CandidatePromotion.status` separately carries the candidate's own *lifecycle* state, which legitimately diverges from a historical edge verdict). **Verdict precedence:** governance is append-only — a re-decision is a new `PromotionDecision` with a new `DECIDED_ON` edge, so a candidate may carry multiple `DECIDED_ON` edges with differing outcomes. The **governing** verdict is the outcome of the edge whose `PromotionDecision` has the **latest `decided_at`** (no `SUPERSEDES` edge type; re-decision is permitted). **Ties are structurally excluded:** the gateway enforces **per-candidate strict `decided_at` monotonicity** on `DECIDED_ON` writes — a new edge on a candidate must carry `decided_at` strictly greater than any existing edge's on that candidate — so "latest `decided_at`" is always well-defined and needs no tie-break (§7 #15). §7 conformance check #15 keys promotion authority off the *governing* approving edge, so a node materialized on a now-superseded approval is detected when the governing verdict has since become a rejection (its un-promotion remediation rides the **retraction shape**, §5 / §7 #21). Batch-eligibility *policy* → DDR-003. **Retraction.** Un-promotion of a promoted KG fact is an **EA-gated reversing `CandidatePromotion` + `RETRACTS` edge** (§5; §7 #21). Its **upstream authority for un-promotion routes to ADR-008 (Ground-Truth Mutation Governance)** (scope: mutation of enterprise ground truth generally — entry *and* un-promotion); ADR-001 §2.5 authorizes promotion only and does not stretch to un-promotion. The *remedy boundary* — supersession (the default, when wrong-but-replaceable; §6 versioning already supports it) vs. retraction (the exception, when the fact should never have been promoted and has no correct replacement) — is **DDR-003 governance**, not authored here; DDR-002 owns only the schema shape. A **post-materialization re-decision of an executed retraction** (the governing verdict on a terminal-`promoted` retraction later flipping — the retraction-side analogue of #15's flip case) is likewise remedy-boundary territory routed to DDR-003: the schema records the re-decision append-only under §2.4's verdict precedence; the un-retraction remedy — and a standing detection analogue of #15 for executed retractions (#21 traces to *an* approving decision, not the *governing* one) — is not authored here.

- **`(:Governance:Actor)`** — `actor_id` (T1 PK) · `actor_type` (T2, indexed — `human` / `system`) · `name` (T3) · provenance (T1)
- **`(:Governance:Role)`** — `role_id` (T1 PK) · `name` (T3) · provenance (T1)
- **`(:Governance:Attestation)`** — `attestation_id` (T1 PK) · `result` (T2, indexed — pass/fail) · `attested_at` (T2) · provenance (T1, `origin_mechanism: authored` — SOFIA-registered) *(closes obligation↔satisfaction, §3)*
- **`(:Governance:Condition)`** *(applicability predicate for `approved_conditional` promotions)* — `condition_id` (T1 PK) · `predicate` (T2 — structured, evaluable predicate over solution/Catalog properties; pattern-aligned with `rule_definition`, carrying a declared `dependency_manifest` — declared-introspectable, consumed at retrieval-time evaluation, §7 #19; no edge-backing obligation, §7 #10) · `created_at` (T2) · provenance (T1, `origin_mechanism: authored` — the same issuing act as its `PromotionDecision`). Linked from its `PromotionDecision` (`HAS_CONDITION`) and governs the conditionally-promoted node's applicability **by traversal** (not duplicated onto the node, beyond the `applicability_state` marker, §5). The knowledge is promoted *with* this condition (not held pre-promotion). A `conditional` node is admitted to a consuming context only if that context **satisfies its `Condition` predicate** (single-condition, §7 check #19; evaluated at retrieval; knowledge-service / solutioning SDDs), which is what lets the same knowledge be reused across qualifying solutions. **Multi-condition → named DDR-003 gap (see *Named Gaps*):** if a fact is conditionally promoted more than once it is structurally **one node reached by multiple `HAS_CONDITION` paths** (forced by `(business_key, version)` uniqueness); the **multi-condition consumption semantics** (disjunctive vs. otherwise) and the **condition-set lifecycle** (activation/retirement/`status`; accrete vs. supersede) are a named gap — DDR-002 adds **no** `Condition.status` field now, resolving them when a real multi-condition case arises. Condition vocabulary → DDR-003.

*Confidence-derivation basis:* **`flat_base`** (basis 2), plane-level — immutable append-only authoritative record; staleness is governed by re-decision/lifecycle, not decay (§2 preamble; DDR-004).

*Key postures:* for `GateDecision` in the current build, the external entity is the system of record and the node is a **captured reference**, not SOFIA's authoritative record (`source_record_ref` carries the external reference; `origin_mechanism: ingested`). For `PromotionDecision`, SOFIA *is* the issuing authority over the feedback loop, so there is no external record to point at (`origin_mechanism: authored`, per the §1 matrix) — the de-conflation keeps the SDLC-gate semantics from leaking onto the promotion gate and vice-versa. **`approved_conditional` on `GateDecision`** is a **faithful mirror** of a real enterprise gate outcome ("approved with conditions"); it does **not** constitute §5 selection (only `approved` does, §5), and its gate conditions are **not** the promotion `Condition` machinery (§2.4/§5) — the external record holds them, reachable via `source_record_ref`, and reusing the promotion predicate would re-introduce the exact gate↔promotion leakage this de-conflation prevents. **Entry-path rule (Governance-plane types):** a **mirrored** external decision enters `origin_mechanism: ingested` + `source_record_ref`; a **SOFIA-issued or SOFIA-registered** decision enters `origin_mechanism: authored` with no `source_record_ref` (`PromotionDecision`, `Condition`, and — thinner inference — `Attestation`). `Actor` is the general decider identity — human, SOFIA, or another external system — each with a structural home; **`Actor`/`Role` sourcing** (deployment-shaped) is routed to the **governance-state-manager SDD**, bounded by this rule. The per-action **disposition-event stream is excluded** (transient → orchestration/observability SoR). `Actor`/`Role` are governance-participating identities, **not** an IAM mirror. Immutable append-only: a re-decision is a new node.

### 2.5 Standards — the governance-of-knowledge graph (versioned + change-impact)

- **`(:Standards:Standard)`** — `standard_id` (T1 PK) · `version` (T1) · `name` (T3) · `authority` (T3) · `status` (T2) · provenance (T1) *(the referenceable authority — "per policy 1234")*
- **`(:Standards:PolicyRule)`** — `policy_rule_id` (T1 PK) · `version` (T1) · `rule_name` (T3) · `domain` (T2, indexed) · `status` (T2, indexed, versioned) · `superseded_by` (T2) · `enforcement_level` (T2 — hard/soft) · `enforced_at_gate` (T2) · `statement` (T3 — natural-language requirement) · `rule_definition` (T3 — opaque declarative IF/THEN evaluation logic, the validator consumes it; the graph never traverses into it) · `dependency_manifest` (T2 — declared, introspectable list of the Catalog entity-types/labels the rule reads; the evaluation logic stays opaque, the manifest does not) · provenance (T1)
- **`(:Standards:ComplianceControl)`** — `control_id` (T1 PK) · `framework` (T2, indexed — HIPAA/SOC2/PCI as *data*) · `control_ref` (T3) · `control_name` (T3) · provenance (T1)

*Confidence-derivation basis:* **`flat_base`** (basis 2), plane-level — versioned authoritative ground truth; staleness is supersession, not decay (§2 preamble; DDR-004).

*Decision (executable rule logic on the node):* the executable rule logic rides the `PolicyRule` node as opaque `rule_definition` (KG-as-policy-SoR, matching DDR-001's served-from-graph model); the constraint-validator evaluates it. **`rule_definition`↔edge sync invariant:** every Catalog entity-type in a rule's declared `dependency_manifest` MUST appear as a `GOVERNED_BY` / `MANDATES` target for that rule — the opaque evaluation logic may not hide a dependency the graph can't see, and because the manifest is introspectable the invariant is mechanically checkable. **Honesty caveat:** §7 check #10 enforces *declared-but-wired* — every manifest entry is edge-backed — **not** *declared-complete* against the still-opaque `rule_definition`. An author who omits a real dependency from the manifest passes #10 while the omission persists. Closing that (a runtime check that a rule reads *only* manifest-declared entities; or extraction from a parseable rule form) is **validator behavior routed to the constraint-validator SDD**, not schema — DDR-002 does not re-open the rule-logic-on-node opacity decision (the same posture the exposure-window takes: name the exposure, track the mechanization). Documented invariant + conformance check (§7). **`GOVERNED_BY`'s role:** the stored, traversable record of *entity-triggered* applicability (a standing Catalog fact — any solution using this entity inherits this rule), keeping governance graph-queryable; the obligation-closure mechanism (entity-triggered traversal ∪ condition-triggered compute, with validator-logic closure) lives in §3. A standard is broader than rules — it can `DEFINE` a `PolicyRule`, `PRESCRIBE` a `Technology`/`Pattern`, etc. **The `Process` node type and its edges are deferred** until the Process use case is designed; `PRESCRIBES` keeps its non-Process targets and loses the Process target until Process lands. Mandates resolve in two ways: **structurally** (validator reads the solution graph — prohibitions, "uses the vault") or **by attestation** (process performed / evidence produced — `Attestation`, §2.4).

### 2.6 Extension mechanism + cost plane (the first Extension registration)

**Mechanism:**
- **`(:Extension:PlaneDefinition)`** — `plane_id` (T1 PK) · `plane_name` (T3) · `version` (T1) · `status` (T2, indexed) · `attaches_to` (T2 — list of core labels the plane may enrich) · `property_schema` (T2 — **structured, validator-consumable** schema descriptor: node-label → property → type/constraints, which schema-on-write enforces) · **`confidence_basis`** (T2 — **structured, validator-consumable** confidence-derivation-basis declaration: node-label → basis (the §2 four-way enumeration), plus the declared freshness operand where the basis is `aging`; basis kind only, no values — semantics and values are DDR-004's) · provenance (T1). *`PlaneDefinition`'s own basis: `flat_base` (basis 2) — versioned registry ground truth; staleness is supersession.*

The registry is self-describing (query `PlaneDefinition` nodes to enumerate registered planes). `attaches_to`, `property_schema`, and `confidence_basis` are **declarations** (properties), not edges-to-labels. Schema-on-write validates extension nodes against their `PlaneDefinition`; instance-edges are bounded to the `attaches_to` labels. Extension nodes carry the plane's secondary label (the hybrid, §1). **`attaches_to`-exists invariant:** every label in a `PlaneDefinition.attaches_to` must reference an existing core label (CI-only, §7). **Declared-totality registration rule:** every node-label a plane declares carries **exactly one** confidence-derivation basis, with `confidence_basis` declaring the **same label set** as `property_schema` (a basis for an undeclared label is as invalid as a label without a basis) — non-citability is declared (`non_citable`), never inferred from omission, so no "citable" predicate exists to evade — and an `aging` label carries its freshness operand; a registration violating any of these **fails** (schema-on-write at plane registration; CI-only mirror, §7 #26). This is what establishes derivation totality at the registration boundary rather than rescuing it at capture. **A well-formed citation of a `non_citable`-class node is rejected at capture, typed `NON_CITABLE_SOURCE`** — a declared-contract steering outcome, distinct from `CONFIDENCE_UNDERIVABLE`, which remains the defense-in-depth backstop for malformed or legacy definitions and is unreachable for any well-formed class (§2 preamble; DDR-004). The cost plane below is the **worked validation exemplar** — standing it up exercises the registration path end-to-end, so the Extension mechanism is *tested by construction*, not merely asserted (the first-registration benefit). This exemplar purpose stands as authored here; the separate question of **cost as a platform capability** (whether and why SOFIA models cost at all) is **routed to the forthcoming cost-capability decision record**, not asserted in this schema.

**Cost plane — first registration:** `PlaneDefinition{plane_id: "cost", attaches_to: [Capability, Technology], confidence_basis: {CapabilityCostEstimate: {basis: native_confidence}, RateCard: {basis: non_citable}, CostFactor: {basis: non_citable}}}` *(each `confidence_basis` value is a **per-label declaration object** `{basis: <kind>, freshness_operand?: <operand>}` — the form §2.6's prose and check #26 require, not a flat `{label: basis-string}` map; the cost plane declares no `aging` label, so no `freshness_operand` appears here — an `aging` label would read e.g. `{basis: aging, freshness_operand: observed_at}`. `property_schema` elided — the elision hides the property detail, not the label set: the registered definition declares the same three labels, so #26's key-set equality holds by construction)*.
- **`(:Cost:RateCard)`** — `rate_card_id` (T1 PK) · `version` (T1) · `effective_from`/`effective_to` (T2) · `status` (T2, indexed) · `rates` (T3, unit→cost payload) · provenance (T1). *Never-deleted; superseded only. Basis: `non_citable` (basis 4) — see below.*
- **`(:Cost:CostFactor)`** — `cost_factor_id` (T1 PK) · `factor_scope` (T2, indexed — technology/capability) · `factor_type` (T2, indexed) · `amount`/`unit` (T3) · `version` (T1) · `status` (T2) · provenance (T1). *Reference input. Basis: `non_citable` (basis 4) — see below.*
- **`(:Cost:CapabilityCostEstimate)`** *(immutable, basis-pinned, as-of-decision record; NOT a refreshable cache)* — `estimate_id` (T1 PK) · `aggregate_cost` (T2 — **derived at creation, then frozen**, never hand-set) · `rate_card_version_ref` / `cost_factor_version_ref` / `capability_version_ref` (T2 — the pinned as-of basis) · `confidence` (T2) · `computed_at` (T2 — the as-of timestamp) · `decision_ref` (T2 — the option/decision this estimate informed) · provenance (T1, `origin_mechanism: derived` · `derivation_class: aggregated`). **Append-only and immutable; never claims currency** — current/hypothetical cost is **compute-on-read** (Tier-1/2 traversal + DTO (data-transfer object), cost-estimation SDD). Persisted for decision-traceability (which option's cost drove a choice, immutably as-of) and estimate-vs-actual variance (the actuals side is external/Environment). **Retention:** as-of records are **justifiably permanent**, not the transient-telemetry anti-pattern — they are *decision-paced and low-volume by design* (one per option per decision, not event-stream-paced) and *needed permanently* (decision-traceability + variance both require the record forever; archiving one would break its variance computation). This is structurally unlike Operational's archivable `resolved`/`superseded` tail: a cost as-of record has no decision-closed terminal state. If volume ever surfaces a query-performance concern at scale, the **volume/query-management affordance** (partitioning, indexing, cold-tiering of decisions whose variance window has closed — *not* deletion) → cost-estimation SDD / DDR-003. *Basis: `native_confidence` (basis 1) — carries its own `confidence`.*

*Cost is the first temporally-mixed plane* (Tier-1/2 here names the cost-estimation tiers — distinct from §1's T1–T3 property tiers; → cost-estimation SDD)*:* `RateCard`/`CostFactor` versioned reference, `CapabilityCostEstimate` an **immutable as-of-decision record** (not refreshable; current cost compute-on-read). Tier-1/2 traversal + the `Tier1CostEstimateResult` DTO → cost-estimation SDD. **The cost-evidence surface is `CapabilityCostEstimate`** — the confidence-bearing as-of-decision record is what a reasoner cites; `RateCard`/`CostFactor` are aggregation inputs, not `Evidence` sources (`non_citable`, basis 4) — the purpose-built substitute exists, so a citation of the raw reference nodes rejects typed (`NON_CITABLE_SOURCE`, above) and steers to the estimate.

---

## 3. KG relationships — edge grammar + catalog

**Edge grammar:**
- **Direction & naming** — `SCREAMING_SNAKE` verb phrases in the natural semantic direction; Neo4j traverses both ways, so direction is for readability, not access. One edge per relationship — no redundant inverses.
- **Edge properties** — attributes belonging to the *relationship*, not either node.
- **Edge provenance** — asserted-fact edges (ingested, e.g. `APPROVED_OPTION_FOR`) carry provenance (`origin_mechanism`, §1); structural edges do not.
- **Re-bind discipline** — each edge *type* declares a supersession behavior, **governed by the point-in-time principle**: edges that record a *point-in-time fact* (a solution's use of a version, evidence citations, decision references) are **`rebind:pinned`** — they never re-point, because re-pointing would falsify the historical record; edges expressing a standing *current* pointer (current-recommended, selection-preference) are **`rebind:current`** — they re-point to the new version on supersession so the hot path stays current. Because every potentially-unbounded edge is pinned by correctness necessity, the `rebind:current` re-point set is **bounded-degree by construction**; the residual — any `rebind:current` type that proved unbounded — would fall back to identity-indirection for that type (not adopted now). Declared per edge type in the catalog below.
- **Cardinality** — not constrainable in Neo4j schema → documented invariant + conformance check.
- **Temporal edges** — relationships whose *validity* is time-bound carry `effective_from`/`effective_to` + `status`; structural edges do not.
- **Confidence** — confidence lives **on the surface whose certainty it describes**: *edge* confidence = certainty of an observed relationship (Environment as-running edges; the Operational `OBSERVED_IN` per-target certainty); *node* confidence = reliability of a derived/reasoned thing (`ObservedPattern` lesson reliability, `CapabilityCostEstimate`, `ReasoningProgress`, `Evidence`). KG *structural* edges are authoritative by default (no confidence). Canonical definition + per-surface composition rule in §4.

**Edge catalog (five core-plane sub-graphs + Cost/Extension):**

*Selection (Catalog) — `rebind:current`:* `(Pattern)-[:REQUIRES_CAPABILITY {required, tier_conditional, tier_threshold}]->(Capability)` · `(Technology)-[:APPROVED_OPTION_FOR {conditional, justification}]->(Capability)` *(temporal)* · `(Pattern)-[:PREFERRED_OVER]->(Pattern)` · `(Technology)-[:REPLACED_BY]->(Technology)` *(temporal)* · `(IacTemplate)-[:IMPLEMENTS]->(Pattern)`

*As-running (Environment, confidence-bearing):* `(DeployedService)-[:RUNS {confidence}]->(Technology)` · `(DeployedService)-[:DEPLOYED_IN {confidence}]->(DeploymentEnvironment)` · `(DeployedService)-[:REALIZES {confidence}]->(Capability)` · `(ConfigurationItem)-[:PART_OF {confidence}]->(DeployedService)`

*Track-record (Operational):* `(ObservedPattern)-[:OBSERVED_IN {confidence}]->(Technology | Pattern | Capability | DeploymentEnvironment)` *(edge `confidence` = per-target observation certainty, distinct from the node's lesson-reliability `confidence` — composed at rollup)*

*Governance-of-knowledge (Standards):* `(Standard)-[:DEFINES]->(PolicyRule)` · `(Standard)-[:PRESCRIBES]->(Technology | Pattern)` *(Process target deferred)* · `(PolicyRule)-[:MAPS_TO]->(ComplianceControl)` · `(PolicyRule)-[:MANDATES]->(Technology)` · `(Pattern | Technology | Capability)-[:GOVERNED_BY]->(PolicyRule)` *(the stored, traversable record of **entity-triggered** applicability — any solution using this entity inherits this rule. Condition-triggered applicability is computed from `rule_definition`; the two combine in the obligation-closure model below.)*

*Decision/audit (Governance):* `(Actor)-[:HAS_ROLE]->(Role)` *(temporal)* · `(Actor)-[:APPROVED {role, approved_at}]->(Decision)` *(targets either subtype via the supertype)* · `(GateDecision)-[:REVIEWED {hash}]->(Artifact)` · `(Attestation)-[:SATISFIES]->(PolicyRule)` · `(Attestation)-[:BY]->(Actor)` · `(Attestation)-[:EVIDENCED_BY]->(Artifact)`. *(`(Attestation)-[:FOR_PROCESS]->(Process)` deferred entirely.)* *Pointer (defined at the keystone, not duplicated here):* the `GateDecision` verdict edge `(GateDecision)-[:DECIDED_ON {outcome}]->(Solution)` is defined at the Solution keystone (§5).

*Cost (Extension):* `(CapabilityCostEstimate)-[:FOR_CAPABILITY]->(Capability)` *(cardinality 1)* · `(CapabilityCostEstimate)-[:HAS_COST_FACTOR]->(CostFactor)` · `(CostFactor)-[:FOR_TECHNOLOGY]->(Technology)` · `(CapabilityCostEstimate)-[:PRICED_BY]->(RateCard)` *(`rebind:pinned`)*

*Solution-centric and promotion edges are deferred to the keystone (§5).*

**Obligation↔satisfaction model (hybrid).** Standards hold the *obligation*; Governance holds the *attestation*; the solution sits in the middle. The **validator computes the obligation set** — *entity-triggered* obligations reached by traversing `GOVERNED_BY`/`MANDATES` from the solution's `USES`/`FOLLOWS` entities, **plus** *condition-triggered* obligations from evaluating `rule_definition` conditions against the solution — then joins each obligation to its satisfaction: validator-structural (prohibitions, structural requirements) or attestation-evidenced (`Attestation`). The **closure is validator logic, not a single graph traversal** (the graph stores the associations, the rule logic, and the attestations). A missing satisfaction surfaces as a compliance gap (an RG risk inference), not silence. Closure logic + manifest consumption → constraint-validator SDD.

---

## 4. The Reasoning Graph — node schemas

The RG types align to ADR-002 §2.6 / ADR-001 §2.2 vocabulary: `ReasoningSession`, `ReasoningProgress`, `Evidence`, `RejectedAlternative`. Per **ADR-002 §2.6**, the rich `ReasoningProgress` *fields* are SDD-level; DDR-002 owns only the schema contract. RG nodes carry **no KG plane label** (§1).

- **`(:Reasoning:ReasoningSession)`** — `session_id` (T1 PK) · `lifecycle_state` (T2 — **AOE (Agent Orchestration Engine)-owned; value-set deferred to the AOE SDD, with a named placeholder; DDR-001 likewise leaves it unspecified**) · `created_at` (T2) · provenance (T1). *(No 1:1 `solution_ref` — the session→solution link is the `PRODUCED` edge (§5), **0..\***: a session may explore multiple candidate solutions or conclude none. Consistent with DDR-001 §Reasoning Graph, which pins one session per run, not one solution per session.)*
- **`(:Reasoning:ReasoningProgress)`** — `progress_id` (T1 PK) · `conclusion_type` (T2, indexed — PatternMatch / TechnologySelection / GapConclusion / OverrideFlag / RiskSignal / ComplianceEvaluation; *(contested)*) · **`reasoner_category`** (T2, indexed — enum `encoded_reasoning` / `specialized_agent` / `llm_advisory` / `human_override`, ADR-001 §2.2's four categories) · **`reasoner_ref`** (T2 — the specific reasoner; **required when `reasoner_category = specialized_agent`**, permitted otherwise — SDDs may tighten) · **`authoritative`** (T2, indexed boolean — the ADR-001 §5.2 read-discipline filter surface; **fixed mapping**: `llm_advisory` → `false`, all other categories → `true`; §7 check #23) · `confidence` (T2) · `overridden_by_human` (T2) · `created_at` (T2) · provenance (T1). *= the typed conclusion (lean reading: no separate container). Rich conclusion/rationale → SDD.*
- **`(:Reasoning:Evidence)`** — `evidence_id` (T1 PK) · `fact_summary` (T2 — denormalized snapshot) · `confidence` (T2, inherited) · `weight` (T2) · `source_node_version` (T2 — version pin) · `observed_at` (T2 — **the capture/snapshot instant**: when this Evidence's observation of the cited node occurred, gateway-clocked; the Δt reference "to" point — the cited node's own `observed_at`, the "from" point, is recovered via `SOURCED_FROM` + the version pin, never mirrored here; DDR-004 §3)
- **`(:Reasoning:RejectedAlternative)`** — `rejected_id` (T1 PK) · `candidate_type` (T2, indexed) · `rejection_reason` (T3) · `score_delta` (T2) · `human_accepted` (T2) · `human_accepted_at` (T2) · provenance (T1, `origin_mechanism: authored`; §1)
- **`(:Reasoning:ProvenanceSummary)`** *(the provenance-survival snapshot)* — `provenance_summary_id` (T1 PK) · **`frozen_evidence_ids`** (T2 — the `evidence_id`s of the **set** of originating `Evidence` the promotion's provenance chain spans; **the correlation key**) · `frozen_fact_summaries` (T2 — the denormalized `fact_summary` content of that set, frozen at materialization so it survives `Evidence` expiry) · `frozen_source_version_pins` (T2 — the corresponding `Evidence.source_node_version` pins, likewise frozen) · **`frozen_source_node_refs`** (T2 — the corresponding cited-source node references, likewise frozen — post-expiry audit knows *what* was cited without the live `Evidence`) · provenance (T1, `origin_mechanism: derived`). **The four `frozen_*` arrays are index-aligned, keyed by `evidence_id`** — position *i* across all four is one per-`Evidence` entry — so live/frozen correlation is mechanical by `evidence_id` (never heuristic pin-matching, which is ambiguous when two `Evidence` cite the same version), and #20's completeness check is set-equality between `frozen_evidence_ids` and the §5-defined span. The **frozen lineage** a post-expiry audit needs is preserved two ways: the *expiring* content (the `Evidence`-set summaries + their source-version pins) is **denormalized onto this node** as a retention-boundary snapshot (§1 — a temporal-fidelity snapshot, **not** hot-path duplication, because the live `Evidence` is deliberately retained-out); the *durable* anchors (the originating `CandidatePromotion` and, through it, the governing `PromotionDecision`) stay **live-traversable** via `MATERIALIZES_PROVENANCE_OF` (§5) and the existing append-only Governance edges (which never expire). It carries **no KG plane label** (not ground truth) and is a `derived` RG-region node. *Materialized **at promotion** (§5, §6), so build-before-`Evidence`-expiry holds by construction.* The internal structuring is fixed above (index-aligned per-`Evidence` entries keyed by `evidence_id`); the affordance warrants **no CI invariant of its own** — frozen-layer reachability is exactly what §7 #20 already guarantees (existence, completeness, at-promotion binding), and a second invariant would re-verify #20 under another name. What remains deferred is the **reverse-lookup index / query-pattern optimization**, pending the retention policy DDR-003 owns (see *Named Gaps*).

**Reasoner attribution + the LLM-advisory invariant.** `reasoner_category` realizes ADR-001 §2.2's per-artifact source attribution by category on `ReasoningProgress`; `authoritative` realizes its authoritative flag as an **explicit, indexed boolean** (the ADR-001 §5.2 read-discipline filter surface) — denormalized-for-introspection and kept honest by the fixed category→flag mapping (§7 check #23), **not** derived at read (the `dependency_manifest` species). The **LLM-advisory invariant:** LLM-rendered content rides an **authored** artifact as **non-authoritative content properties** — never as an authored node of its own — and the hosting artifact's `reasoner_category` / `authoritative` values govern how reads treat it; the rich payload itself stays T3/T4, SDD-shaped.

**Confidence — canonical definition.** Confidence is a `[0.0, 1.0]` scalar living **on the surface whose certainty it describes**: **node** confidence = reliability of a derived/reasoned thing — on `Evidence` *source authority × freshness* (authoritative Catalog above aging Environment, decayed by the cited source's age — its own `observed_at` — as of the capture instant, `Evidence.observed_at`), on `ObservedPattern` the *lesson reliability*, on `CapabilityCostEstimate` the estimate reliability, on `ReasoningProgress` the *rolled-up conclusion*; **edge** confidence = certainty of an observed relationship — Environment as-running edges, and `OBSERVED_IN` *per-target observation certainty*. Thus **Environment = edge-only; Operational = node (lesson reliability) + edge (per-target certainty)**, the two composed at rollup. **Inheritance/rollup rule:** `Evidence.confidence` inherits from the `SOURCED_FROM` KG node's authority class at snapshot time; `ReasoningProgress.confidence` is a (SDD-defined) rollup over its `SUPPORTED_BY` Evidence — DDR-002 fixes that the rollup exists and is **upper-bounded by its strongest supporting evidence path**: the **ceiling = `max` of the supporting `Evidence.confidence`** (a conclusion is no more confident than its strongest evidence path), and **`weight` affects aggregation only *below* the ceiling, never the ceiling itself**; the **zero-evidence case** (a conclusion with no `SUPPORTED_BY` Evidence) is left to the SDD that defines the rollup function. The rollup function itself → SDD (§7 check #24 enforces the ceiling; if the SDD's function redefines path strength, the comparator amends with it). **`Evidence.confidence` presence** is guaranteed on the mediated capture path by DDR-004's derive-or-reject totality (a capture computes the inherited value or rejects the citation typed, never defaulting a null); §7 check #28 graph-state-verifies presence for `Evidence` arriving outside that path, the out-of-path backstop paralleling #23's treatment of the `authoritative` flag.

**`conclusion_type` ↔ correction-surface mapping.** The mapping onto §5's seven correction-surfaces (technology, pattern, capability, integration, risk, compliance, cost) is **two-axis, not a flat values→surfaces arrow**. Four surfaces map **directly** to a `conclusion_type`: technology → `TechnologySelection`, pattern → `PatternMatch`, risk → `RiskSignal`, compliance → `ComplianceEvaluation`. **Capability** homes on **`GapConclusion`** per DDR-001's gap definition (gap = a required capability with no resolving technology — DDR-001 §Two-Graph Model, cross-cutting KG invariants). **Integration and cost** are the stated **absorbed variants** — `TechnologySelection` / `GapConclusion` variants rather than distinct conclusion types. **`OverrideFlag` is flavor-carrying** (the Refine flavor, §5), **orthogonal to the surface axis** — it rides any surface rather than naming one. The mapping aligns with DDR-001's (coarser) signal/action classes. *(The enum remains contested pending the spike-generalization pass.)*

*Write authority (**ADR-002 §2.6** invariant):* ASA authors `ReasoningProgress`; AOE owns `ReasoningSession` lifecycle only; writes route via knowledge-service. Mechanization deferred (§7).
*RG-internal edges:* `(ReasoningSession)-[:CONTAINS]->(ReasoningProgress)` · `(ReasoningProgress)-[:SUPPORTED_BY]->(Evidence)` · `(ReasoningProgress)-[:REJECTED]->(RejectedAlternative)` · `(ReasoningProgress)-[:LED_TO]->(ReasoningProgress)`.
*Point-in-time fidelity:* `fact_summary` + `source_node_version` make the audit trail immune to later KG drift.

**RG provenance posture.** Provenance is present across the RG, but expressed by node kind:
- `ReasoningSession` and `ReasoningProgress` are *authored reasoning state* → they carry the provenance group (provenance = the authoring session/agent, ASA per §2.6).
- `RejectedAlternative` is **authored content** → it carries the provenance group (`origin_mechanism: authored`), because DDR-001 §Reasoning Graph frames it as ASA-authored and the *primary feedback-loop input*; the highest-leverage promotion signal must not carry the weakest provenance.
- `Evidence` is the **sole** *surrogate-only* RG type — the sole derived RG type that omits the group; its provenance is **structural** — it pins `source_node_version`, stamps the capture instant (`observed_at`), and links `SOURCED_FROM` the authoritative KG node it snapshots (§5), written as an **atomic capture-unit** (`Evidence` + `SOURCED_FROM` in one gateway transaction, §7) so the surrogate is **sound by gateway contract** (CI-enforced via §7 check #14, exposure named — not a DB guarantee). This structural attribution — source recoverable by `SOURCED_FROM` + version-pin traversal, authoritativeness inherited-confidence-weighted from the cited KG node's authority class — **satisfies ADR-001 §2.2's source-attribution intent for this type**, without carrying `reasoner_category` / `authoritative` (those are the attribution surface for the *conclusion* type, `ReasoningProgress`).
- `ProvenanceSummary` is a **`derived` RG type that carries the group** (`origin_mechanism: derived`) — it is **not** surrogate-only; it is a materialized derivation whose own origin is recorded, distinct from the `Evidence` structural surrogate it preserves. It therefore joins the §7 existence-constraint scope.

So the provenance *existence constraint* (§7) scopes to **KG nodes + the three authored RG types (`ReasoningSession`, `ReasoningProgress`, `RejectedAlternative`) + `ProvenanceSummary` (derived, group-carrying) + the Artifact family**; `Evidence` remains the **sole** surrogate-only type. **The surrogate's enforceability gap is named, not hidden:** the cardinality invariants that *make* the `Evidence` surrogate sound — including the **atomic capture-unit** — are CI-only (§7); Neo4j cannot express them as constraints, so they are documented and mechanized ahead of the gateway (§7).

**Accountability boundary.** RG content is captured *judgment*, not held to the KG determinism invariant. The EA gate (`PromotionDecision`) is the **accountable-consolidation control** — it converts a judgment-derived proposal into an *accountable, ratified, authoritative* KG fact (per ADR-001 §2.5's "EA-gated consolidation into encoded logic"). It restores **accountability and authoritativeness, not determinism**: determinism is a separate, never-lost property of the gap/override *derivation* (a structural function of graph facts — DDR-001 §Two-Graph Model, cross-cutting KG invariants), independent of how any fact entered. Promoted facts are therefore **in-scope** for that derivation by design (the encoding-density-growth feature — a promoted override becomes sanctioned and future deterministic reasoning reads it) and remain **distinguishable** via `origin_mechanism: promoted`. A promoted-origin KG node must still trace to an **approving** `PromotionDecision` (§7) — that ties it to an *accountable* decision. The *policy* of how the derivation weights promoted-vs-ingested facts, and the *health* of judgment-consolidation over time → DDR-003 / reasoning-machinery.

---

## 5. Cross-graph linkage + the feedback loop

**Ownership line — what DDR-002 owns vs. cites in this section.** DDR-002 **owns** the *schema contract*: the `(:Artifact:Solution)` and `CandidatePromotion` node shapes, the cross-graph and promotion edge grammar, and the *structural* invariants (append-only terminal status, provenance-chain traceability). DDR-002 **cites, and does not re-author:** the promotion *mechanics / data-path* → DDR-001; the *diagnosis policy / EA approval criteria / thresholds / cadence* → DDR-003; the *workflow* → SDD. The Refine/Request-new/Correct-scope framing and the "EA-gate is a diagnosis" characterization below are stated for schema motivation only — their governance is DDR-003's, their data-path is DDR-001's.

**The Artifact family — a node-family within the two-graph store.** `(:Artifact …)` is a **distinct node-family**, *not* a third graph or third citizen-class peer to the KG and RG: the store is the two-graph model (KG + RG, DDR-001), and Artifacts are nodes *within* it that bridge produced deliverables to the reasoning that made them and the catalog they draw on. It is **neither a KG plane nor RG** — the produced-deliverable family (solutions, BCDR plans, build sheets). It carries its own PK, its own provenance group (`origin_mechanism: authored`/`derived`), and its own uniqueness, and it carries **no KG plane label** (so plane-scoped ground-truth traversals skip produced artifacts). It joins KG and RG only by edges. This resolves the question of whether `Solution` is a Catalog node or an RG node: it is **neither** — it is an Artifact node-family member (DDR-001 §Versioning & Temporal Model and §Hybrid Persistence Model — solution dual-home).

**Evidence edges (RG → KG):** `(Evidence)-[:SOURCED_FROM]->(KG node)` *(`rebind:pinned`; confidence inherits here — authoritative Catalog scores above aging Environment)* · `(RejectedAlternative)-[:WOULD_HAVE_USED]->(KG node)` *(`rebind:pinned` — a point-in-time fact per §3's re-bind discipline)*. *(`DREW_FROM`/`TRAVERSED` deferred — the considered-set is reconstructable from Evidence + RejectedAlternative.)*

**The Solution keystone:**
- **`(:Artifact:Solution)`** — `artifact_id` (T1 PK) · `artifact_type` (T2 — `solution`) · `version` (T1) · `lifecycle_state` (T2, indexed — FSM (finite-state-machine): proposed→architected→gated→approved→operational) · `content_hash` (T2 — tamper-detection) · `snapshot_ref` (T2 — dual-home pointer) · `target_environment` (T2 — design-time intent, drawn from the `environment_class` controlled vocabulary) · `created_at` (T2) · provenance (T1, `origin_mechanism: authored`). `(:Artifact)` is the general produced-deliverable family (solutions, BCDR plans, build sheets).

*Bridge edges:* `(ReasoningSession)-[:PRODUCED]->(Solution)` *(**0..\*** — a session may produce multiple candidate options or none)* · `(Solution)-[:USES]->(Technology)` · `(Solution)-[:FOLLOWS]->(Pattern)` · `(GateDecision)-[:DECIDED_ON {outcome}]->(Solution)` · `(Solution)-[:HAS_ARTIFACT]->(Artifact)` *(Solution → component sub-artifacts)*. `target_environment` is a **controlled-vocabulary string** (the `environment_class` value-set) resolved by string-match, **not** an FK (foreign-key)-edge to `DeploymentEnvironment`; the realized environment arrives later via `DeployedService`. Dual-home mechanics → §6.

**Selection among produced candidates.** When a session `PRODUCED`s multiple candidate `Solution`s, **selection is the approving `(GateDecision)-[:DECIDED_ON {outcome: approved}]->(Solution)` edge** plus `lifecycle_state` advancement (`gated → approved`) — **`approved` is the sole selection-constituting outcome** (a mirrored `approved_conditional` records a real enterprise gate outcome faithfully but does **not** constitute selection, §2.4) — **no `selected` flag** (a redundant boolean would re-create the divergence the edge-borne verdict avoids). Before gating there is no recorded selection (selection is a governance act); the ASA's pre-gate *primary-candidate* pick is solutioning-runtime state → solutioning SDD. Non-selected candidates remain `proposed`/`architected` and are retained as the explored option-set. **Boundary — not-selected `Solution` vs `RejectedAlternative`:** a not-selected `Solution` is an **artifact-level** complete candidate deliverable that wasn't gated; a `RejectedAlternative` (§4) is a **decision-step-level** discarded conclusion *within* the reasoning trail. They are different granularities and not interchangeable; "why a candidate wasn't chosen" is reasoning reachable via the gate decision — no new edge.

**CandidatePromotion + feedback loop** — *schema contract owned here; mechanics cited to DDR-001 per the ownership line above:*

- **`(:Reasoning:CandidatePromotion)`** — `candidate_id` (T1 PK) · **`proposal_kind`** (T2, indexed — enum `{promotion, retraction}`; the explicit **direction discriminator**, so property-filtered reads no longer have to traverse to the `RETRACTS` edge to tell forward promotions from reversals) · `promotion_type` (T2, indexed — the promotion's *subject/kind*, distinct from direction) · `proposed_change` (T3 — opaque) · `requested_item` (T3) + `requested_item_kind` (T2 — technology/pattern/capability/integration) + `target_ref` (T3) *(for request-new gaps)* · `basis_strength` (T2 — *contested*) · `confidence` (T2) · `status` (T2, indexed — proposed / under_review / approved / **approved_conditional** / rejected / promoted) *(the candidate's **lifecycle** state; the decision's per-candidate verdict rides the `DECIDED_ON {outcome}` edge — and the two legitimately diverge)* · `created_at` (T2) · provenance (T1). **Append-only**; terminal `status` (`promoted` / `rejected {reason}`) is the durable evaluation outcome — declines are explainable and gaps don't re-surface. A single `PromotionDecision` may decide this candidate as part of a **batch** (1:many `DECIDED_ON {outcome}`); `approved_conditional` carries an applicability `Condition` (§2.4). **Reversing (retraction) variant:** a retraction is the **same `CandidatePromotion` node carrying `proposal_kind: retraction`** plus a `RETRACTS` edge (§Promotion edges) — it goes through the **same EA gate** (`DECIDED_ON` by an approving `PromotionDecision`) as a forward promotion, so un-promotion is accountable, not a bare delete (§7 #21, #25). It reuses the **shared `status` lifecycle** — the FSM tracks **proposal execution**, read uniformly across both `proposal_kind`s: terminal `status: promoted` denotes the proposal's effect was **materialized** (for a retraction: the un-promotion applied — the `RETRACTS` edge written and the target read-excluded — *not* that anything was forward-promoted); this is a deliberate economy, not an accident, and no new `status` value is added. The remedy-boundary *governance* (supersession vs. retraction) is DDR-003; the schema shape is owned here.

- **Provenance-survival guarantee.** A **durable terminal-`promoted`** node's provenance must remain **recoverable after its originating `Evidence` is retained-out**, preserved as **frozen traversable structure** (not an opaque blob — post-expiry audit and derivation-nature recovery stay a traversal, not a parse), and **materialized before** the `Evidence` it summarizes is removed. **Span (the originating-`Evidence` set to freeze):** the **closure over the promotion's `PROPOSED_FROM` targets** to their Evidence-reaching edges — an `Evidence` target contributes itself; a `ReasoningProgress` target contributes its `SUPPORTED_BY` `Evidence` set; an `ObservedPattern` target contributes nothing to freeze (a durable, live-traversable KG node); a `RejectedAlternative` target contributes no `Evidence` (`WOULD_HAVE_USED` is not Evidence-mediated). The two Evidence-reaching paths are `PROPOSED_FROM` (to an `Evidence` directly) and `PROPOSED_FROM` ∘ `SUPPORTED_BY` (via a `ReasoningProgress`); the **gateway computes this closure in the materialization transaction** (future Evidence-reaching edges amend the span — contract maintenance, not a hedge). This is the schema-relevant guarantee DDR-002 commits (grounded in DDR-001 §Versioning & Temporal Model — summary-on-evidence-expiry; the provenance chain's expiry-survival, below, leans on it). The form is a dedicated **`(:Reasoning:ProvenanceSummary)`** node (§4, `origin_mechanism: derived`) — **not** a frozen subgraph — that denormalizes the expiring `Evidence` content (the index-aligned, `evidence_id`-keyed `frozen_*` entry arrays, §4) as a retention-boundary snapshot and stays bound to the durable promotion record by `(:ProvenanceSummary)-[:MATERIALIZES_PROVENANCE_OF]->(CandidatePromotion)`. Materialization is **at-promotion**, so the build-before-`Evidence`-expiry ordering holds **by construction** (it is an invariant of the promotion act, §7 #20 — not a sweep that races retention). The residual gap is the *reverse-lookup index / query-pattern optimization* over `ProvenanceSummary`, to finalize with the retention policy and the first real instance (see *Named Gaps*; the affordance warrants no CI invariant of its own — §4). Framed as a *retention-boundary snapshot*, not hot-path denormalization (§1).

**`applicability_state` marker — on promoted KG nodes.** Every node materialized via `PROMOTES_TO_KNOWLEDGE` carries `applicability_state` (T2, indexed — `unconditional` (default) / `conditional`), **distinct from** the version-lifecycle `status` (a node can be the active version *and* `conditional`). It is the structural key the §7 read-discipline invariant filters on: active ground-truth traversals admit a `conditional` node only where the consuming context satisfies its `Condition` (§2.4, §7 check #19). A `conditional` node is retained and fully traceable — never hard-deleted. **Per-version + supersession carry-forward:** `applicability_state` is **per-version**, established by that version's own promotion decision. Supersession (§6) must **preserve** an EA-set conditional scope — a successor to a `conditional` node is **not** consumed `unconditional` absent an explicit EA `PromotionDecision` re-scoping it (a silent default-to-`unconditional` would over-admit past an EA-set `Condition`, a failure §7 check #19 cannot catch because the successor would no longer be marked `conditional`). DDR-002 commits this integrity expectation and mechanizes it as **§7 check #22** (conditional-scope carry-forward on supersession, predecessor-keyed, safety-critical tier); the carry-forward *mechanism* on supersession (including any cross-origin case) → **knowledge-service SDD / conformance-mechanization**. **Two-surface applicability:** applicability is constrained on two distinct surfaces — **Catalog eligibility** fields (`tier_applicability[]`, `approved_data_classifications[]`) on **ingested** `Technology` (§2.1), and `applicability_state: conditional` + `Condition` on **promoted** knowledge — and **both bind**. Their composition into a single consumption decision ("may I use this here?") is the consuming SDD's responsibility → **knowledge-service / solutioning SDDs**; DDR-002 names the two surfaces, not a unification mechanism.

*Promotion edges:* `(CandidatePromotion)-[:PROPOSED_FROM]->(ReasoningProgress | Evidence | ObservedPattern | RejectedAlternative)` · `(PromotionDecision)-[:DECIDED_ON {outcome}]->(CandidatePromotion)` *(the feedback-loop EA gate — distinct subtype; **1:many** for batch decisions with per-candidate `outcome`)* · `(PromotionDecision)-[:HAS_CONDITION]->(Condition)` *(for `approved_conditional`)* · `(CandidatePromotion)-[:PROMOTES_TO_KNOWLEDGE]->(KG node)` *(materialized node stamped `origin_mechanism: promoted` + `applicability_state`)* · `(ProvenanceSummary)-[:MATERIALIZES_PROVENANCE_OF]->(CandidatePromotion)` *(`rebind:pinned` — the at-promotion provenance snapshot's binding to its durable promotion record, §7 #20)* · `(CandidatePromotion)-[:RETRACTS]->(KG node)` *(`rebind:pinned` — the reversing-CandidatePromotion's un-promotion target; EA-gated, §7 #21)*.

*Structural & read-discipline invariants:*
- **Proposal-visibility (dissolved to DDR-001 + gateway).** `CandidatePromotion` must not appear in ground-truth synthesis traversals until EA-approved. This is **owned by DDR-001** as the proposal-visibility invariant + conformance check #4 (`SOFIA never self-modifies the KG`; promotions excluded from ground-truth traversal until approved). DDR-002 supplies the *structural aid* — `CandidatePromotion` carries no KG plane label, so plane-scoped traversals skip it — but names honestly that full enforcement is **gateway-enforced read-discipline** (the sole-owner knowledge-service scopes ground-truth reads), **not** a pure structural guarantee. Lands in the CI-only set (§7); cites DDR-001 check #4, no DDR-001 amendment needed.
- **Provenance chain.** A promoted node traces back through `PROMOTES_TO_KNOWLEDGE` → `CandidatePromotion` → `PROPOSED_FROM` → `ReasoningProgress`/`Evidence`/`RejectedAlternative` → `SOURCED_FROM` → original facts; `origin_mechanism: promoted` makes it distinguishable from `ingested` (DDR-001 check #6). Its pre-promotion *derivation nature* is recovered by this same traversal to the source, not denormalized. **Expiry survival:** when the originating `Evidence` expires, the chain is preserved by the **provenance-survival guarantee** (above) — frozen traversable structure via the `ProvenanceSummary` node; the *retrieval affordance* over it is the residual named gap. **Approving-decision invariant:** a promoted node must trace, via `PROMOTES_TO_KNOWLEDGE` ← `CandidatePromotion` ← the **governing** approving `DECIDED_ON` edge — the edge whose `PromotionDecision` has the **latest `decided_at`**, with `outcome ∈ {approved, approved_conditional}` — to a `PromotionDecision` (CI-only, §7 #15). Keying off the *governing* edge (not any approving edge) means a node whose governing verdict has since flipped to `rejected` is detected even if a stale earlier `approved` edge exists; its un-promotion remediation is the **retraction shape** (the EA-gated reversing `CandidatePromotion` + `RETRACTS` edge, §7 #21; remedy-boundary governance → DDR-003).
- **Retracted-node read-discipline.** A KG node carrying an inbound `RETRACTS` edge from an **EA-approved** reversing `CandidatePromotion` is excluded from ground-truth synthesis traversals — **gateway-enforced read-discipline keyed on the EA-approved `RETRACTS` edge**, parallel to §7 checks #9 (proposal-visibility) and #19 (conditional-consumption); no denormalized `retracted` status marker (the edge is the structural fact). The retracted node is **retained and fully traceable** (durable rationale on the audit trail) — never hard-deleted.

**Generalized correction mechanism** *(schema motivation; governance → DDR-003).* Every `conclusion_type` is a human-correction surface (technology, pattern, capability, integration, risk, compliance, cost — reconciled to the `conclusion_type` enum in §4), in three flavors: **Refine** (override to an existing option → context-scoped, evidence-backed preference signal on the selection edge, no pairwise cycles; the *runtime preference-landing* → solutioning SDD), **Request-new** (gap → human evaluation/onboarding; never auto-created), **Correct-scope** (false risk / mis-scoped rule → adjust existing ground truth). The capture machinery (`overridden_by_human`, `human_accepted`, `GapConclusion`, `RejectedAlternative`, `CandidatePromotion`, EA-gate, `PROMOTES_TO_KNOWLEDGE`) is general across surfaces. The "override is a signal the EA *diagnoses*" framing is governance characterization owned by DDR-003, not authored here.

---

## 6. Versioning & temporal model

**Per-posture spectrum** (each plane fit to its nature): Catalog/Standards/RateCard **version** (supersession), Environment **ages** (`observed_at`), Operational **distills** (status-managed), Governance **accretes** (immutable append-only), RG **pins** (point-in-time), Solutions/Artifacts **dual-home**.

**Supersession — Option A (scoped to versioned-ground-truth types: Catalog, Standards, RateCard, CostFactor, PlaneDefinition).** A node carries `version` + `effective_from`/`effective_to` + `status` + `superseded_by`; superseding creates a new retained node and marks the old superseded. **Atomicity:** supersession is a **single atomic transaction** — create new version node, mark old `superseded`, and re-point the `rebind:current` edges to the new node — so a traversal never observes a half-superseded state with two active versions. `rebind:pinned` audit/evidence/consumption edges are untouched (they pin to their specific version node). **The re-point is O(degree) but bounded by construction:** the point-in-time principle (§3) forces every potentially-unbounded edge (solution-consumption, evidence, decision-reference) to be `rebind:pinned`, so the `rebind:current` set is limited to standing current-pointer edges (low-degree); the assumption is stated, and the residual — any `rebind:current` type that proved unbounded — falls back to identity-indirection for that type (not adopted now). The edge re-pointing is the **gateway's** responsibility (knowledge-service, sole writer — consistent with DDR-001's gateway pattern; cited, not re-authored). Never-delete via retained versions; as-of via effective-dating. Chosen over a blanket identity+version-node pattern to keep the selection-graph hot path hop-free (versioning is rare; traversal is constant).

**Version-pinning.** `Evidence.source_node_version` + `CapabilityCostEstimate`'s pinned basis (`rate_card_version_ref` / `cost_factor_version_ref` / `capability_version_ref`) reference specific retained version nodes (`rebind:pinned`); with `Evidence.fact_summary` that yields point-in-time fidelity. The immutable as-of cost record is the cost-plane instance of this point-in-time discipline.

**Durable promotion records.** A terminal-`promoted` `CandidatePromotion` is **expiry-exempt** (durable); the **provenance-survival guarantee** (§5) holds that when bounded RG retention expires the originating `Evidence`, the promoted node's traceability is preserved as **frozen traversable structure** (not a blob), materialized before the `Evidence` is removed, keeping post-expiry audit graph-native (schematizing DDR-001's summary-on-evidence-expiry — §Versioning & Temporal Model). The form is a dedicated `(:Reasoning:ProvenanceSummary)` node (§4), materialized **at promotion** via `MATERIALIZES_PROVENANCE_OF` so build-before-expiry holds by construction (§7 #20). The **retention policy** (the `Evidence`-expiry windows the retention classes set) → DDR-003, and the **retrieval index / query-pattern optimization** over `ProvenanceSummary` → knowledge-service SDD (named gap — see *Named Gaps*).

**Dual-home (Artifact/Solution).** The graph node holds metadata, edges, `content_hash`, `snapshot_ref`; immutable content lives in the external snapshot store (Firestore, routed to DDR-001); the hash is tamper-detection (snapshot hash must match `content_hash`). A new version is a new node + new snapshot; the old pair is retained.

**Uniqueness under versioning.** PKs unique on `(business_key, version)`; "exactly one active version per business_key" is a documented invariant + conformance check.

---

## 7. Constraints, indexes & conformance

**DB-enforced (Neo4j Enterprise) — generating rules:**
- **Uniqueness** on every PK; `(business_key, version)` for the versioned-ground-truth types.
- **Existence constraints** on the provenance group (`origin_mechanism`, `recorded_at`) on every **KG** node, the **three authored RG types** (`ReasoningSession`, `ReasoningProgress`, `RejectedAlternative`), the **derived `ProvenanceSummary`** (`origin_mechanism: derived`), and the **Artifact family** (see §4 for the RG-provenance posture) — plus each node's T1 required props. (The Enterprise *edition* committed in ADR-002 buys existence-constraint capability.)
- **Indexes** on the flagged T2 traversal/filter properties only (lean — no speculative indexing); plane secondary-labels are inherently indexed. *The index set is **provisional**, revisitable once the SDDs surface real query patterns.* **Reverse cross-graph lookup:** `SOURCED_FROM` is stored RG→KG, so the reverse direction (KG-node → citing-`Evidence`, "what reasoning cited this fact?") needs an index/affordance — named here; the concrete index lands with the retrieval patterns in the knowledge-service SDD.

**Conformance-checked invariant set — CI-only (Neo4j cannot express → documented + CI; enforcement posture below).** Neo4j existence/uniqueness constraints cannot express these; they are honestly named as CI-only with a stated exposure window (below):

1. **RG-provenance edge invariants (safety-critical tier, sequenced ahead of the gateway):** every `(:Reasoning:Evidence)` carries a **mandatory** `SOURCED_FROM` edge to a provenance-bearing KG node; every `(:Reasoning:RejectedAlternative)` is reached via **exactly one** parent `REJECTED` edge — justified on **per-conclusion** grounds: a `RejectedAlternative` carries per-conclusion rejection rationale (`rejection_reason`/`score_delta`/`human_accepted`), so it *is* the rejection-by-one-conclusion; the same option rejected by two conclusions is two nodes the feedback loop aggregates by their shared `WOULD_HAVE_USED` target (better for recurrence detection than a shared node). `Evidence` **not** sourced from a provenance-bearing KG node is **prohibited** (`Evidence` is KG-sourced by definition and check #14 enforces it). These cardinality invariants are what make the §4 structural provenance-surrogate sound.
2. **no vector properties.**
3. **relationship cardinality** (e.g. `FOR_CAPABILITY` cardinality 1).
4. **one active version per business_key.**
5. **aggregate-derivation** (`CapabilityCostEstimate.aggregate_cost` derived-at-creation-then-frozen, never hand-set).
6. **schema-on-write** (extension nodes validate vs. `PlaneDefinition`; edges bounded to `attaches_to`).
7. **ADR-002 §2.6 write-authority** (ASA authors `ReasoningProgress`; AOE session-lifecycle only).
8. **never-delete** (versioned types supersede; atomic re-bind bounded by the point-in-time rebind assignment).
9. **proposal-visibility / read-discipline:** `CandidatePromotion` excluded from ground-truth synthesis traversal until EA-approved — gateway-enforced read-discipline + structural label-skip aid; defers to DDR-001 check #4.
10. **`rule_definition`↔edge sync:** every Catalog entity-type in a **`PolicyRule`'s** declared `dependency_manifest` appears as a `GOVERNED_BY`/`MANDATES` target — edge-backing is scoped to `PolicyRule` manifests, the case §3's edge grammar defines. A **`Condition`'s** `dependency_manifest` is **declared-introspectable only**: it is consumed by retrieval-time predicate evaluation (#19) and carries no edge-backing obligation (no `GOVERNED_BY`/`MANDATES` grammar exists for `Condition`, §3). **Caveat:** this enforces *declared-but-wired*, not *declared-complete* against the opaque logic; the completeness check (a rule reads only manifest-declared entities) is validator behavior → constraint-validator SDD.
11. **provenance distinguishability (DDR-001 check #6):** `promoted` distinguishable from `ingested` via `origin_mechanism`.
12. **tamper-detection** (`Artifact.content_hash` matches snapshot hash).
13. **no-PHI** (classification, gateway-enforced). **Layer reconciliation:** the *runtime mechanism* is gateway-enforced per §1 / DDR-001 (classification strips PHI data at the write boundary); this *conformance contract* is **gateway-behavioral** — 1b-specified, required-flip at the gateway build. Classification enforcement and the sole write path are **co-located in the gateway**, so no PHI-bearing exposure interval precedes enforcement at graph entry (ADR-002 §2.5 / DDR-001 gateway prohibitions — the zero-exposure-window property; scoped to graph entry, since intake-side classification on pre-graph workflow state is service behavior outside this schema's scope).
14. **atomic capture-unit:** an `Evidence` node and its `SOURCED_FROM` edge are written in a single gateway transaction — the structural provenance surrogate is **sound by gateway contract, CI-enforced, exposure named** (not a DB guarantee) at the sole-writer choke-point (joins #1; safety-critical tier).
15. **promoted-origin → governing approving decision:** every `origin_mechanism: promoted` KG node traces, via `PROMOTES_TO_KNOWLEDGE` ← `CandidatePromotion` ← the **governing `DECIDED_ON` edge** — the edge whose `PromotionDecision` has the **latest `decided_at`**, with `outcome ∈ {approved, approved_conditional}` — to a `PromotionDecision`. Keyed off the *governing* (not any) approving edge, so a node whose governing verdict has flipped to `rejected` is caught despite a stale earlier `approved`; correct for single and batch decisions alike. **Well-definedness:** the governing edge is total (uniquely defined) under **per-candidate strict `decided_at` monotonicity** on `DECIDED_ON` writes (gateway-enforced, §2.4) — ties are structurally excluded, so "latest `decided_at`" never underdetermines the governing verdict.
16. **exactly-one-subtype-per-Decision:** every `Decision` carries exactly one of `{GateDecision, PromotionDecision}` — never bare, never both.
17. **ingested ⇒ source_record_ref:** every `origin_mechanism: ingested` node carries a `source_record_ref`; **every `derivation_class: distilled` node** also carries a `source_record_ref` to its external source (scoped to `distilled` — `aggregated` derives from internal version-pins, no external ref). Aligns §1's "applicable on distilled" with §7 enforcement.
18. **attaches_to-exists:** every label in a `PlaneDefinition.attaches_to` references an existing core label.
19. **conditional-consumption read-discipline:** an `applicability_state: conditional` node is admitted to a consuming context only if that context **satisfies its `Condition` predicate** (single-condition; retrieval-enforced) — parallel to #9's proposal-visibility read-discipline; gateway-enforced at retrieval. The blocking safety control: conditional knowledge is never consumed out-of-scope. *(Multi-condition consumption semantics — named gap, §2.4 / Named Gaps.)*
20. **at-promotion ProvenanceSummary materialization + completeness:** every durable terminal-`promoted` **promotion-kind** (`proposal_kind: promotion`) `CandidatePromotion` carries a `(:Reasoning:ProvenanceSummary)` written via `MATERIALIZES_PROVENANCE_OF` **at promotion time** — before any originating `Evidence` can expire, so the build-before-expiry ordering holds **by construction** (a gateway-behavioral materialization check) — and the summary is **complete against the §5-defined span** (the closure over `PROPOSED_FROM` targets), not merely present. **Promotion-kind scoping:** the quantifier is promotion-kind only. A `proposal_kind: retraction` candidate also reaches terminal `status: promoted` under §5's shared-status economy (there, terminal `status: promoted` denotes the un-promotion *executed*, not a forward promotion), but it materializes no KG node and builds no `ProvenanceSummary` (only the promotion path does, §5); its own post-retention audit survival rides §6 expiry-exemption + its durable rationale props (§2.4), never a summary. So the literal all-`promoted` reading would sweep a case that by design carries no summary — the scoping is the coherent reading, not a narrowing. Makes the §5 provenance-survival guarantee enforceable rather than aspirational. *(Follow tier — enforced with the gateway, not ahead of it; it guards post-expiry audit survival, not facts-entering-ground-truth.)*
21. **retraction-gated:** a `RETRACTS` edge is valid only from a `CandidatePromotion` (`proposal_kind: retraction`) that traces — via `DECIDED_ON` — to an **approving** `PromotionDecision` (`outcome ∈ {approved, approved_conditional}`), with durable rationale on the audit trail. The **un-promotion mirror of #15**: un-promotion of authoritative ground truth is EA-gated, never a bare delete. The retracted node's exclusion from ground-truth reads is the gateway-enforced read-discipline keyed on this EA-approved edge (§5; parallel to #9/#19). *(Safety-critical tier — guards ground-truth integrity against unauthorized mutation; mechanized as a gateway-behavioral contract.)*
22. **conditional-scope carry-forward on supersession:** every supersession of an `applicability_state: conditional` node yields a successor that either carries `conditional` (condition preserved or EA-re-scoped) or was explicitly re-scoped `unconditional` by an EA `PromotionDecision`. **Predecessor-keyed** — the check keys on the `conditional` predecessor, catching the silent default-to-`unconditional` that #19 cannot (its successor would no longer be marked `conditional`). *(Safety-critical tier — guards conditional knowledge against over-admission past an EA-set scope; gateway-behavioral, mechanized as the one atomic supersession transaction per §6.)*
23. **flag↔category consistency:** every `ReasoningProgress` carries an `authoritative` value matching the fixed `reasoner_category` mapping (`llm_advisory` → `false`, all other categories → `true`). *(Safety-critical tier — guards against wrong-consumption of non-authoritative content as authoritative, the ADR-001 §5.2 read-discipline surface; dependency_manifest species — denormalized-for-introspection, sync-checked.)*
24. **rollup upper bound:** `ReasoningProgress.confidence ≤ max(SUPPORTED_BY Evidence.confidence)`, scoped to conclusions with supporting evidence carrying a non-null `confidence` — where every supporting `Evidence.confidence` is null the ceiling is undefined (`max` over nulls) and #24 does not fire; that presence gap is owned by **#28**, not folded into this comparator (the two concerns stay distinct — comparator vs. presence). *(Follow tier — guards reasoning-quality integrity, not ground-truth entry; graph-state. Comparator bound to §4's canon; amends with the SDD's rollup function if it redefines path strength.)*
25. **`proposal_kind` ↔ `RETRACTS`-edge consistency (scoped to executed proposals):** a `CandidatePromotion` with `proposal_kind: retraction` at terminal `status: promoted` carries a `RETRACTS` edge; and a `RETRACTS` edge originates only from a `proposal_kind: retraction` node. A retraction proposal before materialization carries no `RETRACTS` edge — a checked direction of this invariant: the edge's writing is the EA-gated act (#21; §5). *(Follow tier — guards ledger consumption/audit, not ground-truth entry; dependency_manifest species.)*
26. **basis-declaration totality:** every `PlaneDefinition` declares exactly one confidence-derivation basis per declared node-label, with **`confidence_basis`'s label set equal to `property_schema`'s** — no label without a basis, no basis for an undeclared label — and a declared freshness operand is present iff — and only iff — the label's basis is `aging` (a stray operand on a non-`aging` label is a malformed declaration; §2.6's declared-totality registration rule — non-citability is declared, never inferred from omission). *(Follow tier — a plane that slipped registration without a basis yields fail-closed capture rejections, not ground-truth corruption; graph-state mirror of the gateway's registration validation.)*
27. **basis-admissibility:** every `SOURCED_FROM` edge terminates at a node whose class declares a citable basis (`native_confidence` / `flat_base` / `aging`); a `non_citable`-class label carries no inbound `SOURCED_FROM`. *(Follow tier — a violation is a wrongly-admitted citation, an audit/reasoning-quality concern, not ground-truth entry; the standing form of the §2.6 capture-time rejection.)*
28. **`Evidence.confidence` presence:** every `(:Reasoning:Evidence)` node carries a non-null `confidence`. Presence is otherwise unowned by this set: `confidence` is T2, so the DB-existence constraints (which cover only the provenance group + T1 required props) do not force it; the mediated capture path guarantees a value **by construction** — DDR-004's derive-or-reject totality computes the inherited value or rejects the citation typed (including the branch-(i) null-native reject, DDR-004 §1), never defaulting a null — but no graph-state check verifies presence on an `Evidence` node that arrived **outside** that path, the shape 1a graph-state exists to catch (the #23 pattern, applied to the confidence surface). It also closes the interaction #24 names: an all-null supporting set leaves #24's `max()` ceiling undefined, so #24 skips that conclusion by construction; #28 catches the null `Evidence` **directly**, independent of whether it yet supports any conclusion (schema-legal unlinked `Evidence` exists, §4). *(Follow tier — graph-state; guards reasoning-quality integrity, not ground-truth entry — the #24 class, not the #23 safety-critical class. 1a-only: the write-time guarantee is **subsumed** by DDR-004's derive-or-reject totality — now null-safe via the branch-(i) reject (DDR-004 §1) — an already-specified capture contract, so #28 carries no separate 1b write contract of its own; it is purely the out-of-path graph-state backstop.)*

**Named exposure window + sequencing.** This CI-only set (28) is not Neo4j-expressible; its mechanization is **tiered**, not uniform. **Criterion:** an invariant is *precede-gateway* if the gateway operating without it enforced would, from its first writes/reads, let a safety-or-integrity-critical violation enter authoritative ground truth or synthesis reads that is hard to detect or reverse — unapproved/self-modified KG facts, wrong-consumption of non-ground-truth (proposal/conditional) as ground truth, provenance-broken or PHI-bearing writes, or unauthorized writes. **Safety-critical tier (ahead of the gateway):** #1 + #14 (provenance soundness), #7 (write-authority), #11 + #17 (provenance stamping), #15 (no unapproved/superseded-verdict promotion), **#21 (no unauthorized un-promotion — the #15 mirror)**, #9 (proposal-visibility), #19 (conditional read-discipline), **#22 (conditional-scope carry-forward — the #19 supersession mirror)**, **#23 (flag↔category consistency)**, #16 (Decision well-formedness), #13 (no-PHI). **Follow tier:** #2, #3, #4, #5, #6, #8, #10, #12, #18, **#20 (at-promotion ProvenanceSummary materialization + completeness — gateway-behavioral, enforced with the gateway)**, **#24 (rollup upper bound)**, **#25 (`proposal_kind` ↔ `RETRACTS` consistency, scoped to executed proposals)**, **#26 (basis-declaration totality)**, **#27 (basis-admissibility)**, **#28 (`Evidence.confidence` presence)**.

**Authority split (re-drawn).** DDR-002 **binds** the safety-critical **classification** — *which* integrity properties guard against unapproved/self-modified facts entering authoritative ground truth (schema-contract metadata). The precede-gateway commitment is stated as a **conformance requirement**: *the gateway must not operate against an unenforced safety-critical tier.* The **realization sequencing** (CI-job mapping, within-tier ordering, when each check flips to required) is routed to **build planning / conformance-mechanization**, subject to that binding — it may not downgrade a safety-critical classification or let a safety-critical invariant slip the gateway gate.

**Current state (marker-level).** The graph conformance harness is built (`conformance/`; its build increment is tracked in *Cross-References*): the **safety tier is Increment-1 mechanized** — **1a** graph-state assertions enforce on landing; **1b** gateway-behavioral contracts are specified, required-flip reserved to the gateway build. The **follow tier is Increment 2**, a later BUILD leg. The v1.2.0 batch's new safety-critical checks (#22, #23) and follow-tier checks (#20 completeness, #24, #25) join the harness **catch-up increment** (tracked in *Cross-References*), which also refreshes `schema_constants.py` to the then-current ratified vocabulary. The v1.3.0 additions — the re-scoped #25, #26 (basis-declaration totality), #27 (basis-admissibility), the `confidence_basis` declaration, and the §2 basis enumeration — join the same catch-up increment. The v1.4.0 addition — #28 (`Evidence.confidence` presence, follow tier) — is authored here; its 1a graph-state assertion and the `schema_constants.py` refresh to the v1.4.0 vocabulary **join this batch's harness-mechanization delta** (routed in *Cross-References*, as #26/#27 rode the catch-up increment), landing with the batch's mechanization delivery — stated conditional until that delta lands, not asserted already-enforced. It is **1a-only** — no separate 1b write contract, since the write-time guarantee is DDR-004's derive-or-reject totality, now null-safe via the branch-(i) reject — so no gateway required-flip attaches beyond the follow-tier BUILD leg's activation. **Layer note (no-PHI, #13):** the runtime PHI mechanism is gateway-enforced per §1 / DDR-001; the conformance contract is gateway-behavioral (1b-specified, required-flip at the gateway build); classification and the sole write path are co-located in the gateway, so no PHI-bearing exposure interval precedes enforcement at graph entry.

---

## Pre-Acceptance Conditions

**No pre-acceptance conditions. Ruling stands as authored.** **No Migration Path** (greenfield — no prior schema to migrate from).

---

## Cross-References

**Boundary Routing Map** (named here, owned elsewhere):

- **→ DDR-001** (one-way): plane-model architecture, Extension *architecture*, gateway pattern, three-store persistence, feedback-loop *architecture* (data-path / promotion mechanics), proposal-visibility invariant + DDR-001 check #4, provenance-distinguishability DDR-001 check #6, the Operational-plane durable-distillation definition §2.3 aligns to. Cited, never re-opened.
- **→ DDR-003** (forthcoming, unauthored)**:** feedback-loop *governance* — EA authority, diagnosis policy, what-gets-promoted criteria, thresholds, cadence, retention/audit policy; **telemetry→`ObservedPattern` distillation process**; **CandidatePromotion authorship + EA-gate completion**; `Condition` governance + the conditional→`promoted` transition, batch-promotion eligibility; the **terminal-state `ObservedPattern` archival *policy*** (the `archived` `status` value is schema, §2.3; the when/dwell-trigger/cadence is DDR-003's); the **retraction *remedy-boundary*** (supersession-vs-retraction — the reversing-`CandidatePromotion` + `RETRACTS` *shape* is schema, §5/§7 #21); the **`ProvenanceSummary` retention *policy*** (the `Evidence`-expiry windows; the snapshot *form* is schema, §4); **remaining named-gap hand-off** — multi-condition consumption semantics + condition-set lifecycle (see *Named Gaps*); judgment-consolidation health/weighting.
- **→ ADR-008 (Ground-Truth Mutation Governance):** the **distillation-write checkpoint posture** (whether a `derived` `ObservedPattern` write warrants a human-accountable checkpoint before Operational-plane entry, §2.3) and the **un-promotion / retraction upstream authority** (§2.4 / §5) — scope: mutation of enterprise ground truth generally, entry *and* un-promotion.
- **→ knowledge-service SDD:** gateway write endpoints; enforcement of the invariants (derivation, never-delete + atomic re-bind, schema-on-write, classification, supersession edge re-pointing); **graph-native retrieval affordance** (no-vector positive half — query patterns, similarity-edge question); **conditional read-discipline** (admit `applicability_state: conditional` nodes only where the context satisfies its `Condition`, §7 check #19); **`applicability_state` supersession carry-forward** (the mechanism preserving conditional scope across version supersession, + whether it warrants a CI invariant); **two-surface applicability composition** (compose Catalog eligibility on ingested `Technology` with `Condition` on promoted knowledge — with the solutioning SDD); the **`ProvenanceSummary` reverse-lookup index / query-pattern optimization** (over the snapshot node; retention policy with DDR-003 — the structuring and CI-invariant clauses closed at v1.3.0, §4 / *Named Gaps*); the **reverse cross-graph lookup index** (KG→citing-`Evidence`). *Built against the **safety-critical-tier** contract enforced ahead of the gateway.*
- **→ AOE SDD:** `ReasoningSession.lifecycle_state` value-set.
- **→ solutioning-agent / ASA SDD:** selection-edge **preference-landing** runtime (Refine → `PREFERRED_OVER` re-weight/re-point, `rebind` behavior); retrieval consumption; **conditional-knowledge consumption** (respect the `Condition` when using `conditional` knowledge, and compose it with Catalog eligibility on ingested `Technology` per the two-surface model); **pre-gate primary-candidate selection** (the ASA's runtime pick among `PRODUCED` candidates, before the gate records selection); full `ReasoningProgress` fields (ADR-002 §2.6).
- **→ constraint-validator SDD:** `rule_definition`/`dependency_manifest` evaluation, the obligation-closure logic (entity-triggered traversal ∪ condition-triggered compute, validator-logic closure), the shared `Condition`-predicate evaluation, and the **manifest-completeness runtime check** (a rule reads only its manifest-declared entities — closing the §7 check #10 declared-but-wired gap).
- **→ cost-estimation SDD:** Tier-1/2 traversal, cost DTOs; **compute-on-read** current/hypothetical cost over the immutable as-of estimate records; **estimate-vs-actual variance** (consumes the basis-pinned records + an external/Environment actuals source); **cost-record volume/query-management** (partitioning/indexing/cold-tiering, *not* deletion — with DDR-003).
- **→ reasoning-machinery (parked):** judgment-consolidation **health/weighting policy** — whether and how the gap/override derivation weights `promoted` (judgment-seeded) facts vs. `ingested` ground truth; the determinism/accountability boundary is schema-settled here, the weighting is downstream.
- **→ conformance-mechanization:** the graph conformance harness is **built** (`conformance/`, RBT-33 Increment 1 — safety tier: 1a enforced on landing, 1b contracts specified, required-flip at the gateway build; follow tier: Increment 2, later BUILD leg). **Finalizes the *mechanization*** (CI-job mapping, within-tier ordering, required-flips) **subject to DDR-002's binding classification + precede-gateway rule** — it may not downgrade a safety-critical classification or let a safety-critical invariant slip the gateway gate. The **catch-up increment (RBT-48)** extends the harness with the v1.2.0 batch's checks — #22 (carry-forward, safety-critical), #23 (flag↔category, safety-critical), #20 completeness + #24 (rollup) + #25 (`proposal_kind`↔`RETRACTS`) (follow) — plus the v1.3.0 additions (#25 as re-scoped, #26 basis-declaration totality, #27 basis-admissibility, all follow) — and refreshes `schema_constants.py` to the current ratified vocabulary (`proposal_kind`, `reasoner_category` / `reasoner_ref` / `authoritative`, the `confidence_basis` declaration + the §2 basis enumeration + `NON_CITABLE_SOURCE`, and the existing `ProvenanceSummary` / `RETRACTS` / `MATERIALIZES_PROVENANCE_OF` / `archived` set). The **v1.4.0 delta** extends the harness with #28's 1a graph-state assertion (`Evidence.confidence` presence, follow tier) and refreshes `schema_constants.py` to the v1.4.0 vocabulary — landing with this batch's mechanization delivery (as #26/#27 rode the RBT-48 catch-up increment), not before.
- **→ DDR-004 (inherited-confidence derivation):** the derivation **semantics, function form, constants (invariant-vs-contested treatment, all values), and calibration governance** for the confidence-derivation bases this schema declares (§2 preamble, §2.1–2.6). The declarations here are DDR-004 §4's commissioned schema carriage (DDR-004 Pre-Acceptance Conditions 1–2 discharged at v1.3.0). Cited, never re-authored.
- **→ Process-node work:** the `Process` node type + its deferred edges (`PRESCRIBES`→Process, `FOR_PROCESS`→Process) land when the Process use case is designed.
- **→ gate-enum reconcile:** `GateDecision.gate` placeholder → enterprise SDLC gate taxonomy.

**Upstream implemented:** ADR-001 v1.2.0 (reasoning architecture / Position 4–5), ADR-002 v1.2.0 (graph system-of-record), DDR-001 v1.4.0 (data architecture — the architecture half of the split).

**Standards:** house conventions — data-classification tiers; deployment-runtime exception (ADR-002 §2.2).

### Named gaps

These are *in-scope capabilities deliberately not locked* in this version because they have no empirical instance yet and securing them now would over-specify. Each is re-instated by **additive DDR-002 amendment** when a real instance arises:

- **Multi-condition consumption semantics + condition-set lifecycle** — single-condition consumption is locked (§7 check #19); the multi-`HAS_CONDITION` consumption rule (disjunctive vs. otherwise) and condition activation/retirement/`status` defer; no `Condition.status` field now. The DDR-003 safe interim posture is *surface-to-EA as a scope-conflict, never silent auto-composition*. → DDR-003 (governance) + a future DDR-002 amendment (schema) on first real instance.
- **Provenance-survival retrieval index/affordance** — the snapshot *form* is the `(:Reasoning:ProvenanceSummary)` node, materialized at-promotion (§4/§5/§7 #20); its internal structuring is fixed (index-aligned per-`Evidence` entries keyed by `evidence_id`, §4); the affordance warrants no CI invariant of its own (#20 already guarantees existence, completeness, and at-promotion binding; §4). What defers is the **reverse-lookup index / query-pattern optimization**, pending the retention policy DDR-003 owns. → knowledge-service SDD.
- **Solution-level aggregate confidence** — a single-criterion candidate-comparison score at the pre-gate pick (ASA-computed at synthesis). Requires per-candidate reasoning-attribution linkage (the current RG edge grammar — `CONTAINS` / `SUPPORTED_BY` / `LED_TO` — is session/conclusion-scoped and supplies none), a rollup-scope rule, and a write-authority disposition (the T-09 / Artifact-authority question). Precedent: `aggregate_cost` (derived-at-creation-frozen; §7 check #5 species). Additive on first real instance → solutioning SDD.
- **Gate-decision origin evolution** — the `GateDecision.origin_mechanism` trajectory. **Current:** `ingested` (external SoR, mirrored). **Future-1:** approval *issued in* SOFIA → `origin_mechanism: authored`, human `Actor` — a live §1 value, #17-exempt, genuinely no re-architecture; preconditions: the approval + governance-state-manager SDD designs (RBT-21/22). **Future-2:** SOFIA-*issued* authoritative approval — preconditions: (1) an upstream Position-4 gate-authority ruling (whether/when SOFIA authors authoritative gate decisions — likely the encoding-strategy / Position-4 records in Later), (2) a write-authority home (the same non-enumerated-authoritative-writes question as the Solution-aggregate-confidence disposition), (3) an ADR-001 reasoner-taxonomy category. The provenance axis already supports the value; realization is additive. **Joined at v1.3.0 (cold-read gate): the governing-verdict rule for re-decided `GateDecision`s** — verdict precedence (latest `decided_at`, monotonicity-backed) is ruled only for `PromotionDecision` (§2.4); which gate verdict governs a `Solution` carrying conflicting `DECIDED_ON` outcomes is unruled, and SDD-001 declines widening as an upstream question — routed here with the origin-evolution question, resolved by the same forthcoming approval/governance-state-manager designs.
- **Conditionally-approved-Solution semantics** — the lifecycle-advancement policy for a mirrored `approved_conditional` `GateDecision` (and whether a gate-condition structure is ever warranted, distinct from the promotion `Condition` machinery §2.4/§5). → governance-state-manager + solutioning SDDs; additive on first real mirrored instance.

### Conscious exclusions / deferred scope (boundary chosen, not missed)

- Attestation time-decay / re-attestation cadence
- Waiver / risk-acceptance closure of mandates
- Aggregate / portfolio-level mandates
- Inherited / platform-level compliance
- Full bitemporal *relationship*-history (node as-of + evidence pinning are preserved)
- The `Process` node type + its edges
- `DREW_FROM` / `TRAVERSED` reasoning-trail edges

---

## Layer of Abstraction & Substrate Stability

This DDR operates at the **data-substrate / schema layer** — it fixes the node/relationship/constraint/index contract over the data architecture DDR-001 establishes, gating the downstream service SDDs and cited by DDR-003 (governance). Schema churn is expensive downstream, so the completeness posture is deliberately reversibility-scoped: T1/T2 are locked now; T3/T4 stay schema-on-write and SDD-level.

Amendments to the locked T1/T2 contract — node keys, the provenance two-axis model, edge grammar and `rebind` discipline, the supersession/temporal model, or the CI-only invariant set — carry blast radius into those consumers and require coordinated review. The named gaps (above) are the designated additive-amendment surface; re-instating one is an additive amendment, not a breaking change. The one-way dependency direction to DDR-001 is explicit: DDR-002 depends on DDR-001; DDR-001 does not depend on DDR-002's schema.

---

## Change Log

| Version | Date | Ticket | Change |
|---|---|---|---|
| 1.6.0 | 2026-07-18 | RBT-68 | **Pin-currency re-pin sweep — no schema, contract, or decision change.** References + *Cross-References* Upstream-implemented pins refreshed to the versions the RBT-59 / ADR-008 pointer-resolution batch produced: ADR-001 v1.1.0 → v1.2.0, DDR-001 v1.3.0 → v1.4.0. **ADR-008 v1.0.0** added to the References metadata table (the RBT-59 batch resolved the body pointers to ADR-008 as accepted upstream authority — §2.3 / §2.4 / §5 and the *Cross-References* boundary-routing map — but left the formal table-pin to lag, exactly as the RBT-53 / DDR-004 References add lagged to RBT-54). ADR-002's pin was already at v1.2.0 (caught up in the RBT-62 batch). No content the pins carry changed; a pure currency touch does not restale sibling pins. MINOR per the corpus no-patch-species convention. |
| 1.5.0 | 2026-07-17 | RBT-59 | **KG-entry-governance ADR pointer resolved to ADR-008 (Ground-Truth Mutation Governance), ACCEPTED v1.0.0 in this batch; no decision change.** The three body pointers to the "forthcoming KG-entry-governance ADR" — §2.3 distillation-write checkpoint routing, §2.4/§5 un-promotion/retraction upstream authority, and the *Cross-References* boundary-routing-map entry — resolve to ADR-008, now the accepted upstream authority for ground-truth mutation. Routing prose and § pointers preserved; the historical v1.2.0 Change-Log rows recording the original routing are left as authored. Pin cascade (records still pinning the pre-bump versions) routed to a follow-up ticket. No schema, contract, or decision change. |
| 1.4.0 | 2026-07-15 | RBT-62 | **Additive MINOR amendment — `Evidence.confidence` presence ownership (O-1; decision-bearing).** New §7 check **#28** (`Evidence.confidence` presence, follow tier): every `(:Reasoning:Evidence)` carries a non-null `confidence`; CI-only set 27 → 28. Resolves the ownership question the check-set had not answered — presence is T2 (uncovered by the DB-existence constraints), guaranteed on the mediated path by DDR-004's derive-or-reject totality but unverified for out-of-path arrivals (the #23 pattern applied to the confidence surface), and the all-null supporting set escapes #24's ceiling comparator by construction. 1a-only (no 1b write contract — the mediated path rejects a null at capture); §4 confidence-canon gains the presence clause; #24 gains the null-ceiling scoping naming #28 as owner (resolves the harness's named `obs O-1` gap). No new field or label — no schema perturbation. **Honest floor: n=0 capture traffic; both coverage holes are hypothetical, closed pre-BUILD on the ratified #23/#24 precedent — a stated threshold override, ratified per item 2026-07-15.** **Override discharge (why it holds, not merely that it was ratified):** the #23 precedent is on-point — 1a graph-state exists *precisely* to catch arrivals outside the mediated write path despite the sole-writer gateway (defense-in-depth already ratified there), so a follow-tier backstop of the same species is *a fortiori* proportionate to the safety-critical one; and the branch-(i) root-cause (DDR-004 v1.3.0) shows the presence hole is not purely hypothetical — a null-native inheritance would propagate a null on the mediated path, now prevented at capture and backstopped out-of-path by #28. Decision-bearing. |
| 1.4.0 | 2026-07-15 | RBT-62 | **De-drift, no decision change (F-1 + §2.6 illustration; co-riders on the same touch).** #20's quantifier glossed to **promotion-kind** (`proposal_kind: promotion`) — the literal all-`promoted` reading swept `proposal_kind: retraction` terminal-`promoted` candidates, which by design build no `ProvenanceSummary` (§5); the harness already scopes to promotion-kind, so the text catches up to the mechanization. §2.6 cost-plane registration illustration corrected from the flat `{label: basis-string}` map to the nested per-label declaration-object form (`{basis: <kind>, freshness_operand? …}`) that §2.6 prose and check #26 require — the flat form was the RBT-58 seed-load trap; an `aging`-shape example folded into the illustration gloss. Cosmetic/de-drift class; no schema, contract, or harness change. Also folds a pin catch-up surfaced in review: References + Upstream-implemented ADR-002 v1.1.0 → v1.2.0, matching the §Standards runtime reference the RBT-63 rider already realigned (the version pin lagged the body). Ratified per item 2026-07-15. |
| 1.3.0 | 2026-07-15 | RBT-63 | **Coherence rider (ADR-002 v1.2.0 runtime amendment; no decision change):** the §Standards runtime reference re-points to ADR-002 §2.2's now environment-differentiated deployment runtime (development on managed Aura, production deferred); "self-managed-GKE exception" as the fixed runtime no longer holds. Cross-reference realignment only. |
| 1.3.0 | 2026-07-13 | RBT-54 | Additive MINOR amendment batch, Touch 1 of 3 (record: `agent-loop/deliberation/ddr-002-amendment-batch/record.md`; opened by run-011 union disposition Items 3+4, ratified 2026-07-05; all batch rulings ratified per item 2026-07-13). **#25 scoping clause:** the forward direction scoped to executed proposals — a `proposal_kind: retraction` candidate carries its `RETRACTS` edge at terminal `status: promoted`, keyed to §5's execution gloss; reverse direction unscoped; #21 untouched. Dissolves the #21/#25 joint unsatisfiability in the pre-decision retraction window (the 708bcb75 rider, run-010 audit) and the permanent false positive on legitimately rejected retractions. Latent-defect fix in this document's own text, not accommodation of any SDD. Pre-landing three-hat drafting check + coherence pass over the batch folded (adjudications A-1–A-14 ratified per item, 2026-07-13; carrier: the deliberation record). Decision-bearing. |
| 1.3.0 | 2026-07-13 | RBT-54 | **Touch 2 — ProvenanceSummary internal structuring:** `frozen_evidence_ids` (correlation key) + `frozen_source_node_refs` added; the four `frozen_*` arrays declared index-aligned, keyed by `evidence_id` — mechanical live/frozen correlation, #20 completeness as set-equality against the §5 span (§4/§5). CI-invariant clause closed (no invariant of its own — #20 already guarantees existence/completeness/binding); named gap narrowed to the reverse-lookup / query-pattern optimization pending DDR-003's retention policy. **Honest floor: no valid finding forced this touch — opened on economy** (disposition Item 4; both draws' credits verified the proposed content held as proposed); zero materializations to date — form-fixing pre-traffic, no constants involved; closed ahead of a real instance on the ratified economy warrant, a **ruled exception** to the Rationale's empirical-warrant discipline (the SDD-001 §3.3.8 correlation contract supplied the concrete consumer). Decision-bearing. |
| 1.3.0 | 2026-07-13 | RBT-54 | **Touch 3 — per-class confidence-derivation totality (mechanizes DDR-004; discharges its Pre-Acceptance Conditions 1–2):** §2 preamble — four-basis enumeration, resolution rule, citable-universe boundary, and the citation-scoped clause (a basis governs derivation when a node is cited; evidence completeness is never a derivation concern); §2.1–2.5 plane-level basis annotations with per-label overrides — `DeploymentEnvironment` `flat_base`, its `observed_at`-absence ratified correct-by-design (resolves the run-012 reachable-`CONFIDENCE_UNDERIVABLE` find); §2.6 `PlaneDefinition.confidence_basis` declaration + declared-totality registration rule + cost-plane registration and annotations (`CapabilityCostEstimate` basis 1; `RateCard`/`CostFactor` basis 4 — closes the cee27acc reachable hole by construction) + the `NON_CITABLE_SOURCE` typed rejection (distinct from the `CONFIDENCE_UNDERIVABLE` backstop, demoted to defense-in-depth); §4 `Evidence.observed_at` glossed as the capture instant (author-confirmed intent; DDR-004 Condition 2 discharged by the clarification path — no new field) with the §4 canonical-definition decay parenthetical disambiguated; new §7 checks #26 (basis-declaration totality) + #27 (basis-admissibility), follow tier, CI-only set 25 → 27; Decision.11; DDR-004 boundary-routing entry; `PlaneDefinition` itself declared `flat_base` (batch ruling, adjudication A-3 — a disposition DDR-004 §4's table does not yet carry; alignment noted for that record's next touch). **No values in schema** — basis kinds and operands only; semantics/values are DDR-004's (drift-proofing). **Honest floor: citation reachability for `DeploymentEnvironment` and `RateCard`/`CostFactor` unverified to date; fail-closed safe regardless.** Decision-bearing. |
| 1.3.0 | 2026-07-13 | RBT-54 | **Review-driven revisions from the run-017 four-hat draw** (gen-8 instrument; disposition ratified per item 2026-07-13; carriers: the run-017 ledger + audit). **#10 Condition-half scoped:** edge-backing confined to `PolicyRule` manifests — the case §3's edge grammar defines; a `Condition`'s manifest is declared-introspectable, consumed at retrieval-time evaluation (#19), no edge obligation (§2.4/§3 mirrors aligned; resolves a latent defect in the v1.2.0 extension of #10, which required edge structure never defined for `Condition`). **§2.4 retraction re-decision** named and routed to the DDR-003 remedy boundary (the retraction-side analogue of #15's flip case). **#26 key-set equality:** `confidence_basis` declares the same label set as `property_schema` (registration-rule mirror aligned). **§2.6 exemplar literal** glossed (`property_schema` elided in illustration). **Touch-2 row** gains the ruled-exception clause against the Rationale's empirical-warrant discipline. Decision-bearing (#10 scoping). |
| 1.3.0 | 2026-07-13 | RBT-54 | **Cold-read gate corrections** (stranger-test gate per the governing skill, run 2026-07-13; adjudications CR-1–CR-9 ratified per item): cross-document qualifier added (SDD-001 §3.3.8); §7 #13 citation corrected to ADR-002 §2.5 (fetch-verified); §2.3 routing pointer corrected to the boundary routing map; **the governing-verdict rule for re-decided `GateDecision`s named and joined to the gate-decision origin evolution Named Gap** (the set's one closed-looking unruled surface); #26 only-if warrant stated; §7 marker-paragraph ticket identifiers relocated to *Cross-References* (body subject-named — the F-i inventory disposition); first-use acronym expansions folded, incl. the CI/`ConfigurationItem` disambiguation and the Tier-1/2 vs T1–T3 gloss. No decision change except the gap-naming. Corpus-wide acronym/citation sweep → named Reboot follow-up. |
| 1.3.0 | 2026-07-13 | RBT-54 | **Verification-draw folds (run-018, gen-8; dispositions P18-1–P18-7 ratified per item 2026-07-13; carriers: the run-018 ledger + audit):** §2.6 exemplar gloss states the label-set identity explicitly, so #26's key-set equality is readable from the illustration (resolves a three-hat family on the prior gloss); the §2.4 retraction re-decision clause names the deferred **detection** analogue of #15 for executed retractions (gap-naming — remedy and detection both ride the DDR-003 routing; #21 traces to an approving, not the governing, decision); `WOULD_HAVE_USED` gains its explicit `rebind:pinned` annotation (§5 — the §3 point-in-time principle already forced it). No decision change. |
| 1.2.0 | 2026-07-03 | — | Triage-001 amendment batch (record: `agent-loop/triage/triage-001-distilled-set/record.md`). **Capture-contract realization (T-17, centerpiece):** `ReasoningProgress` gains `reasoner_category` (ADR-001 §2.2's four categories), `reasoner_ref` (required for `specialized_agent`), and an explicit indexed `authoritative` boolean (fixed category→flag mapping); the LLM-advisory invariant is stated (LLM content rides authored artifacts as non-authoritative properties, never authored nodes); `Evidence`'s structural attribution reconciled as satisfying ADR-001 §2.2's intent for that type. New §7 check #23 (flag↔category, safety-critical). Decision addition. |
| 1.2.0 | 2026-07-03 | — | **Governance-plane provenance + gate de-conflation (T-06/T-19/T-21/T-22):** `GateDecision` glossed as the enterprise SDLC gate on produced Solutions (external authority mirrored, `origin_mechanism: ingested`); the authored-reserved clause and intermediate-case aside removed (re-instatement → the consolidated *gate-decision origin evolution* Named Gap). `PromotionDecision` gains explicit `origin_mechanism: authored` (human EA), `Condition` and `Attestation` likewise; the §1 matrix gains the `authored → primary` SOFIA-recorded-human-decision note; an entry-path rule is stated for Governance-plane types. `approved_conditional` clarified as a faithful non-selecting mirror, gate conditions kept distinct from the promotion `Condition` machinery. Decision-bearing. |
| 1.2.0 | 2026-07-03 | — | **Checkable-by-construction invariants (T-13/T-20/T-26/T-27/T-28):** new §7 checks #22 (conditional-scope carry-forward, safety-critical), #24 (rollup upper bound, follow), #25 (`proposal_kind`↔`RETRACTS`, follow); #15 gains the well-definedness clause and §2.4 verdict-precedence the per-candidate strict-`decided_at`-monotonicity rule (ties structurally excluded); #20 gains the completeness clause and §5 the provenance-survival span (closure over `PROPOSED_FROM`); §4 rollup gains the comparator pin (ceiling = max supporting `Evidence.confidence`); `CandidatePromotion` gains `proposal_kind {promotion, retraction}` as the explicit direction discriminator, reconciling the prior `promotion_type: retraction` overload. Decision-bearing. |
| 1.2.0 | 2026-07-03 | — | **KG-entry checkpoint + cost-capability routing (T-02/T-08):** §2.3 strikes the staging-tier substantiation and the "non-EA-gated" characterization — the distillation-write checkpoint posture is explicitly un-decided, routed to the forthcoming KG-entry-governance ADR (pollution controls untouched); §2.4/§5 retraction's un-promotion upstream authority routed there too. §2.6 separates its two justifications — the Extension-validation exemplar stands as authored, the cost-as-platform-capability purpose routed to the forthcoming cost-capability decision record. Decision-bearing. |
| 1.2.0 | 2026-07-03 | — | **Decision-block completeness + Named Gaps (T-07/T-01):** Decision block gains components 7–10 (retraction shape; provenance-survival guarantee; `applicability_state` conditional-consumption model; Option A supersession). Named Gaps gains *Solution-level aggregate confidence* (T-01), the consolidated *gate-decision origin evolution* (T-06 + T-19), and *conditionally-approved-Solution semantics* (T-21). No decision change — enumeration / consolidation of live rulings. |
| 1.2.0 | 2026-07-03 | — | **Status-marker honesty (T-15/T-18, no decision change):** §7 exposure-window re-drawn to marker level — the graph conformance harness is built (RBT-33 Increment 1; 1a enforced, 1b specified, follow tier = Increment 2); the authority-split restated as a conformance requirement (the gateway must not operate against an unenforced safety-critical tier), realization sequencing routed to build planning; the no-PHI layer reconciliation added (#13 + exposure paragraph). |
| 1.2.0 | 2026-07-03 | — | **Reconciliation / no decision change (T-10/T-16 + resolvable tails):** §4 `conclusion_type`↔correction-surface mapping rewritten to its true two-axis structure (T-10); §3 gains the `DECIDED_ON` verdict-edge pointer entry (T-16); §1 `source_record_ref` applicability aligned with §7 #17 vocabulary (required on `ingested` and on `distilled`, permitted on `aggregated`); the GateDecision entry cross-links §7 #17; "twelve SDDs" → "the downstream service SDDs"; DDR-003 and the KG-entry-governance ADR cross-references carry forthcoming/unauthored markers; upstream references bumped to ADR-001 v1.1.0 / ADR-002 v1.1.0 / DDR-001 v1.3.0. |
| 1.1.0 | 2026-07-01 | — | Distilled to standalone contract form; ledger coupling, review-diary residue, amendment/POC-lineage narration, and forward-pointer ticket tokens removed; upstream DDR-001 reference updated to v1.2.0; cross-references re-keyed to doc names; documentation-purity pass folded in (confidence-rollup terminology corrected, scoping-matrix overclaim softened, BCDR expanded, one-way reference corrected to dependency, two overlapping closing sections merged, Date normalized); no decision change. |
| 1.1.0 | 2026-06-22 | RBT-43 | Additive MINOR amendment: three feedback-loop schema realizations — `ObservedPattern.status` gains `archived`; `(:Reasoning:ProvenanceSummary)` node + `MATERIALIZES_PROVENANCE_OF` edge + at-promotion invariant #20; reversing `CandidatePromotion` + `RETRACTS` edge + retraction-gated invariant #21. CI-only invariant set 19 → 21. |
| 1.0.0 | 2026-06-21 | RBT-13 | Initial authoring from the ratified design substrate; ACCEPTED. |
