The finding names the §2.2 table Environment row. Ruling D gives two conformance items, but the finding's locus and claim target the "at retrieval" mischaracterization. Ruling D item (1) fixes the control description; item (2) relabels the "Built" status. Both are part of the same locus (the Environment row cells named in the finding). Let me focus on the exact edit the authority determines.

The finding's locus names both the "Realization routes to: Built..." cell and the "Control-type: Per-policy reliability-weighting at retrieval" cell. Ruling D determines: drop "at retrieval," state capture-time-frozen aging + §6 governance; and relabel "Built" to "form built (DDR-004); constants contested/unvalidated (n=0)."

Ruling D item (1) says "drop 'reliability-weighting at retrieval'; state capture-time-frozen aging + §6 governance." This is a determinate instruction. However, the exact wording of "state capture-time-frozen aging + §6 governance" leaves phrasing latitude. But the core determinate change — removing "at retrieval" and replacing with capture-time-frozen — is authorized. The precise new text still requires composition. Let me weigh: the ruling is prescriptive enough for the control-type cell.

The Control-type cell text is: `Per-policy reliability-**weighting** at retrieval; **no** per-write gate`. Ruling D determines dropping "at retrieval" and stating capture-time-frozen. The minimal conforming edit within the cell is well-determined.

{
  "action": "edit",
  "finding_id": "ef063aa4e596a64e",
  "target": "ADR-008",
  "old_string": "| **Environment** — CMDB-observed deployed reality | Per-policy reliability-**weighting** at retrieval; **no** per-write gate | Per-policy | Built — the inherited-confidence derivation ruling's aging basis |",
  "new_string": "| **Environment** — CMDB-observed deployed reality | Per-policy reliability-**weighting** — capture-time-frozen aging (derived at Evidence capture, never recomputed at read), under the inherited-confidence derivation ruling's two-tier calibration governance; **no** per-write gate | Per-policy | Form built (the inherited-confidence derivation ruling); constants contested/unvalidated (n=0) — its aging basis |",
  "authority_conformed": "ADR-008-run-025-operator-rulings Ruling D, item (1) (drop 'reliability-weighting at retrieval'; state capture-time-frozen aging + §6 governance) and item (2) (relabel §2.2 'Built' to 'form built (DDR-004); constants contested/unvalidated (n=0)'), grounded in DDR-004 §1/§3 (capture-time-frozen, never recomputed at read) and §6 (two-tier calibration governance)",
  "rationale": "Ruling D determines both cells of the Environment row: the control-type must drop 'at retrieval' and state capture-time-frozen aging plus §6 governance, and the 'Built' realization label must be relabeled to 'form built; constants contested/unvalidated (n=0)'. DDR-004 §1/§3 fix the aging as derived-at-capture and never recomputed at read.",
  "confidence": "medium"
}