The finding names two conformance actions in Ruling D, but I can only make one minimal scoped edit. The finding's locus is the Environment row of the §2.2 table. Ruling D §(1) determines the control-description correction for that row; §(2) (relabeling "Built" status) is a separate cell. The finding's claim and locus center on the "at retrieval" contradiction. I'll conform the control-type cell, which Ruling D uniquely determines: drop "reliability-weighting at retrieval", state capture-time-frozen aging + §6 governance.

However, Ruling D specifies both the control-type cell AND the "Built" cell relabel as its conformance, and the finding's locus explicitly names both cells ("Realization routes to" and "Control-type"). A single find-replace can span both if they are within one contiguous table row. The Environment row is one line.

{
  "action": "edit",
  "finding_id": "ef063aa4e596a64e",
  "target": "ADR-008",
  "old_string": "| **Environment** — CMDB-observed deployed reality | Per-policy reliability-**weighting** at retrieval; **no** per-write gate | Per-policy | Built — the inherited-confidence derivation ruling's aging basis |",
  "new_string": "| **Environment** — CMDB-observed deployed reality | Per-policy reliability-**weighting** by capture-time-frozen aging (the inherited-confidence derivation ruling's `base × exp(−Δt/τ)` derived at Evidence capture and never recomputed at read); human accountability at that ruling's two-tier calibration governance of the contested constants; **no** per-write gate | Per-policy | Form built (the inherited-confidence derivation ruling); constants contested/unvalidated (n=0) — the aging basis |",
  "authority_conformed": "ADR-008-run-025-operator-rulings Ruling D (conformance items 1 and 2), grounded in DDR-004 §1/§3 (capture-time-frozen aging, never recomputed at read) and §6 (two-tier calibration governance)",
  "rationale": "Ruling D directs (1) dropping 'reliability-weighting at retrieval' and stating capture-time-frozen aging + §6 governance, and (2) relabeling the 'Built' status to 'form built; constants contested/unvalidated (n=0)'. Both cells are on the single Environment table row, so one scoped replacement conforms the row to the ruling's exact wording.",
  "confidence": "high"
}