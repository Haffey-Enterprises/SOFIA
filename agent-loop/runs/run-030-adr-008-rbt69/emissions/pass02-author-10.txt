{"action":"edit","finding_id":"fe0a8ff6406a32eb","target":"ADR-008","old_string":"§2.4 / §5 (`PromotionDecision`, the retraction shape, remedy boundary routed to the governance design), §6 (version pinning / never-delete), and the constraint set (promoted-origin, retraction-gated, conditional-scope carry-forward, never-delete).","new_string":"§2.4 / §5 (`PromotionDecision`, the retraction shape, remedy boundary routed to the governance design), §6 (version pinning / never-delete), and the constraint set (promoted-origin, retraction-gated — which traces to *an* approving decision, not the *governing* one, so the governing-verdict detection analogue for executed retractions is a named follow-up (§5.3), conditional-scope carry-forward, never-delete).","authority_conformed":"Ruling F (qualify to match the §5.3 concession; governing-verdict detection analogue is the named follow-up) + ADR-008 §5.3 / DDR-002 §7 #21","rationale":"Ruling F directs qualifying the flat retraction-gated claim to match §5.3's concession that the built invariant traces to an approving, not governing, decision, with the detection analogue named as follow-up.","confidence":"medium"}